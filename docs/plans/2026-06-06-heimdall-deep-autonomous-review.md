# Heimdall Deep Autonomous Review & Work Queue — 2026-06-06

> **SUPERSEDED / HISTORICAL PLAN — DO NOT EXECUTE VERBATIM.** This review is retained for audit context only. Use the current work queue and active docs (`README.md`, `DEPLOYMENT.md`, `CLAUDE.md`, `.github/workflows/ci.yml`, `Dockerfile`, and `ansible-playbook.yml`) for release operations.

> **For Hermes:** Use `subagent-driven-development` for implementation batches. Keep each change on `staging`, use Node 22 (`source ~/.nvm/nvm.sh && nvm use 22`), commit in small focused chunks, and do **not** claim CI/deploy/live success without exact-SHA verification.

**Goal:** Comprehensive read-only review of the entire Heimdall project and extraction of every credible autonomous task that can be done while the user is away.

**Scope:** `/Users/reidar/Projectos/Heimdall`, branch `staging`, latest verified staging SHA `d92f2ebdbfe8cdc96dc8386d7cd701dc8b46aeea`.

**Non-claims:** This review did not deploy, publish a GHCR image, merge to `master`, verify `dev.thorchain.no`, or verify live `bond.thorchain.no`. Staging CI run `27069005525` was already verified separately for SHA `d92f2eb...`; the local review here only verifies local gates and source evidence.

---

## 1. Executive summary

- Specialist audit tracks completed: **6** — Security/API, financial correctness, frontend UX/a11y, tests/quality gates, CI/deploy/docs, maintainability/code quality.
- Findings captured with file/line evidence: **104** total.
- Autonomous task candidates captured: **83** total.
- Clearly safe unattended tasks from the specialist reports: **74**.
- Hold / needs user decision, secret, external infra, or non-prod target: **9**.
- Highest-risk product correctness issues are financial: Keplr `MsgDeposit` registration/broadcast uncertainty, BOND operator-fee validation gaps, APY unit heuristics, historical APY double division, and cross-page zero-APY fallback inconsistency.
- Highest-leverage safe work while unattended: add/strengthen tests first, then fix P0 financial math/memo validation, then API hardening, then a11y/UI correctness, then maintainability/docs cleanup.

### Counts by domain

| Domain | Findings | Autonomous tasks |
|---|---:|---:|
| Security/API | 11 | 8 |
| Financial correctness | 12 | 12 |
| Frontend UX/a11y/product | 29 | 15 |
| Test suite/quality gates | 18 | 18 |
| CI/CD deployment/docs | 12 | 12 |
| Maintainability/code quality | 22 | 18 |

### Findings by severity

| Severity label | Count |
|---|---:|
| Critical | 1 |
| High | 27 |
| Medium | 62 |
| Low | 14 |

---

## 2. Verification ledger

Local commands run from `/Users/reidar/Projectos/Heimdall` with Node 22.22.3:

| Gate | Result | Evidence |
|---|---|---|
| `npm audit --omit=dev --audit-level=moderate` | Passed | `found 0 vulnerabilities` |
| `npm run lint -- --max-warnings=0` | Passed | no lint errors reported |
| `npm test` | Passed | 39 test files / 190 tests passed |
| `npm run test:coverage -- --coverage.reporter=json-summary` | Passed | 39 test files / 190 tests passed; coverage generated |
| Coverage snapshot | Informational, not gated | lines 34.36%, statements 32.82%, functions 29.57%, branches 28.61% |
| `npx tsc --noEmit --pretty false` | Passed | no TypeScript errors |
| `npm run build` | Passed | Next.js 16.2.7 production build compiled and generated 18 static pages |
| `npm run e2e` | Passed | 62 Playwright tests passed |
| `npm outdated --long` | Informational | several packages have wanted/latest updates; no task depends on upgrading them first |
| Recent GitHub run check | Informational | staging run `27069005525` for `d92f2eb...` completed success (verified earlier) |

Known local-gate noise to clean up: expected-negative tests currently print stderr from `use-watchlist` invalid JSON and `coinapi` missing-key fallback.

---

## 3. Recommended while-away execution order

This is the safest queue if implementation begins while the user is absent. It avoids secrets, live wallets, production deploys, human policy decisions, and irreversible repo/deploy changes. Commit after each small task or tightly-coupled pair; run the listed focused tests plus the full local gate before pushing.

### Batch 0 — gate hygiene before code fixes

**Why first/now:** Make the test harness less capable of lying before touching financial/security code.

| Domain task | Priority | Files/area | Verification |
|---|---|---|---|
| `tests:AT-01` | P0 | `vitest.config.ts`; optionally `src/test/vitest-config.test.ts` | `npm test` |
| `tests:AT-03` | P0 | `vitest.config.ts`, `src/setupTests.ts`, `src/test/setup.ts`, `src/test/msw/server.ts` | `npm test` |
| `tests:AT-05` | P1 | `playwright.config.ts` | `npm run e2e` and `CI=true npm run e2e` |
| `tests:AT-14` | P1 | `src/lib/hooks/__tests__/use-watchlist.test.ts`, `src/lib/api/__tests__/coinapi.test.ts` | `npm test 2> /tmp/heimdall-vitest-stderr.log; test ! -s /tmp/heimdall-vitest-stderr.log` |
| `tests:AT-15` | P2 | `src/app/api/thorchain/__tests__/route.test.ts`, `src/app/api/midgard/__tests__/route.test.ts` | `npx vitest run src/app/api/thorchain/__tests__/route.test.ts src/app/api/midgard/__tests__/route.test.ts` |

### Batch 1 — P0 financial math/memo correctness that needs no live wallet

**Why first/now:** Highest product-trust ROI. Avoid live broadcasting; use unit tests and mocked wallet paths.

| Domain task | Priority | Files/area | Verification |
|---|---|---|---|
| `financial:T-002` | P0 | `src/lib/transactions/bond.ts`, `src/components/dashboard/transaction-composer.tsx`, `src/lib/transactions/bond.test.ts` | Tests for `BOND:<node>`, `BOND:<node>:<provider>:0`, `10000`, `10001`, empty provider plus fee, leading zeros, whitespace. |
| `financial:T-003` | P0 | `src/lib/utils/calculations.ts`, `src/lib/utils/calculations.test.ts`, `src/lib/types/node.ts`, `src/lib/utils/yield-benchmarks.ts` | Tests for raw base units `1`, `9999999`, `10000000`, large awards, decimal `0.6334`, malformed/negative values, operator fee and provider-share interaction. |
| `financial:T-004` | P0 | `src/lib/hooks/use-historical-apy.ts`, add `src/lib/hooks/__tests__/use-historical-apy.test.ts` or pure helper test | Known-value tests: 10 RUNE/day on 1000 RUNE active bond, multiple intervals, zero totalActiveBond, nanosecond/second timestamp irrelevance. |
| `financial:T-005` | P0 | `src/lib/utils/fee-calculations.ts`, `src/lib/utils/__tests__/fee-calculations.test.ts`, `src/app/dashboard/rewards/page.tsx`, `src/app/dashboard/portfolio/page.tsx` | Cross-page helper tests for zero-yield, mixed zero/nonzero, missing APY, baseline unavailable, and all-zero portfolios. |
| `financial:T-011` | P1 | `src/lib/hooks/use-transaction-history.ts`, `src/lib/utils/tax-export.ts`, new `src/lib/utils/thorchain-memo.ts`, related tests | Tests for uppercase/lowercase memos, memo-only UNBOND base units, malformed memo amount, refund metadata precedence, LEAVE without amount. |
| `financial:T-012` | P2 | `src/lib/utils/formatters.ts`, `src/lib/utils/formatters.test.ts`, financial call sites touched above | Edge tests for max safe integer raw strings, 8-decimal boundaries, negative display, NaN/Infinity, and no precision loss in string conversions. |

### Batch 2 — API hardening without new infrastructure

**Why first/now:** Clamp inputs, no-store sensitive responses, make errors/CORS less leaky. Excludes Redis/shared limiter.

| Domain task | Priority | Files/area | Verification |
|---|---|---|---|
| `security-api:T-03` | P1 | `src/app/api/tax-report/route.ts`; `src/lib/utils/tax-export.ts`; `src/lib/utils/__tests__/tax-export.test.ts` | `npm test -- src/lib/utils/__tests__/tax-export.test.ts src/app/api`; add tests for malformed JSON, over-large or wrong content type, and date range boundaries. |
| `security-api:T-04` | P1 | `src/app/api/midgard/[...path]/route.ts`; `src/app/api/thorchain/[...path]/route.ts`; `src/app/api/midgard/__tests__/route.test.ts`; `src/app/api/thorchain/__tests__/route.test.ts` | `npm test -- src/app/api/midgard src/app/api/thorchain`; add tests that valid queries pass and unknown, negative, huge, or malformed params return 400 without calling `fetch`. |
| `security-api:T-05` | P1 | `src/app/api/address/[address]/route.ts`; `src/app/api/address/[address]/route.test.ts`; `src/app/api/pools/[pool]/route.ts` | `npm test -- src/app/api/address`; add pools route tests or a new colocated test for invalid pool IDs and no upstream call. |
| `security-api:T-06` | P1 | `src/lib/api/cors.ts`; `src/app/api/tax-report/route.ts`; `src/app/api/address/[address]/route.ts`; `src/app/api/pools/[pool]/route.ts`; related route tests | `npm test -- src/app/api`; assert `Cache-Control: no-store, max-age=0` on tax/address/pools and existing proxy cache values remain unchanged. |
| `security-api:T-07` | P2 | `src/lib/api/cors.ts`; `src/app/api/tax-report/route.ts`; route tests under `src/app/api/**` | `npm test -- src/app/api`; add preflight tests for tax POST and production-mode tests that localhost is not allowed unless configured. |
| `security-api:T-08` | P2 | `src/app/api/midgard/[...path]/route.ts`; `src/app/api/thorchain/[...path]/route.ts`; `src/lib/api/client.ts`; proxy route tests | `npm test -- src/app/api/midgard src/app/api/thorchain`; add assertions that 403/502 bodies contain no upstream URL and no attacker-controlled path. |

### Batch 3 — high-value accessible UI fixes

**Why first/now:** Local browser/Playwright verifiable; no credentials or production deploy needed.

| Domain task | Priority | Files/area | Verification |
|---|---|---|---|
| `frontend-ux:TASK-FE-001` | P0 | `src/components/shared/address-input.tsx`, `e2e/homepage.spec.ts` | No live QA needed; browser e2e recommended. |
| `frontend-ux:TASK-FE-003` | P0 | `src/app/dashboard/nodes/page.tsx`, `e2e/dashboard-pages.spec.ts` or new `e2e/nodes.spec.ts` | Browser QA recommended. |
| `frontend-ux:TASK-FE-004` | P0 | `src/app/dashboard/changelogs/page.tsx`, `src/app/dashboard/changelogs/page.test.tsx`, new `e2e/changelogs.spec.ts` | Browser QA recommended for keyboard. |
| `frontend-ux:TASK-FE-005` | P0 | `src/app/dashboard/rewards/page.tsx`, `src/components/ui/dialog.tsx` if adding primitive, `e2e/tax-export.spec.ts` | Browser QA recommended. |
| `frontend-ux:TASK-FE-008` | P1 | `src/app/dashboard/transactions/page.tsx`, `src/components/dashboard/transaction-composer.tsx`, `e2e/transactions.spec.ts` | No. |
| `frontend-ux:TASK-FE-010` | P1 | `src/app/dashboard/risk/page.tsx`, risk component tests, `e2e/risk-security.spec.ts` | No. |
| `frontend-ux:TASK-FE-012` | P0 | `src/app/learn/[slug]/page.tsx`, `src/app/learn/page.tsx`, `src/app/learn/layout.tsx`, new `e2e/learn.spec.ts` | No live QA. |
| `frontend-ux:TASK-FE-013` | P1 | `src/app/page.tsx`, `src/app/dashboard/layout.tsx`, `e2e/redirects.spec.ts`, `e2e/homepage.spec.ts` | Browser QA helpful. |
| `frontend-ux:TASK-FE-014` | P0 | `e2e/homepage.spec.ts`, `e2e/dashboard-navigation.spec.ts`, new `e2e/a11y.spec.ts`, new `e2e/changelogs.spec.ts`, new `e2e/settings-notifications.spec.ts`, new `e2e/learn.spec.ts` | Browser QA via Playwright required. |

### Batch 4 — targeted missing regression tests

**Why first/now:** Turns review findings into RED tests; safe but may take time.

| Domain task | Priority | Files/area | Verification |
|---|---|---|---|
| `tests:AT-06` | P1 | `e2e/wallet.spec.ts` | `npx playwright test e2e/wallet.spec.ts` |
| `tests:AT-07` | P1 | `e2e/comprehensive.spec.ts` | `npx playwright test e2e/comprehensive.spec.ts -g "retries failed requests"` |
| `tests:AT-08` | P2 | `e2e/dashboard-navigation.spec.ts` | `npx playwright test e2e/dashboard-navigation.spec.ts -g "highlights active"` |
| `tests:AT-09` | P2 | `e2e/homepage.spec.ts` | `npx playwright test e2e/homepage.spec.ts -g "Shield"` |
| `tests:AT-10` | P0 | `src/components/dashboard/transaction-composer.test.tsx` | `npx vitest run src/components/dashboard/transaction-composer.test.tsx --coverage` |
| `tests:AT-11` | P1 | `src/components/dashboard/bond-simulator.test.tsx` | `npx vitest run src/components/dashboard/bond-simulator.test.tsx --coverage` |
| `tests:AT-12` | P1 | `src/lib/hooks/__tests__/use-wallet.test.ts` | `npx vitest run src/lib/hooks/__tests__/use-wallet.test.ts` |
| `tests:AT-13` | P0 | `src/app/api/address/[address]/route.test.ts` | `npx vitest run 'src/app/api/address/[address]/route.test.ts'` |
| `tests:AT-16` | P2 | `src/components/shared/api-health-banner.test.tsx` or extend existing API-health component tests | `npx vitest run src/components/shared/api-health-banner.test.tsx` |
| `tests:AT-17` | P2 | `src/lib/hooks/__tests__/use-api-health.test.ts` | `npx vitest run src/lib/hooks/__tests__/use-api-health.test.ts` |
| `tests:AT-18` | P2 | `src/lib/utils/health-score.test.ts`, `src/lib/utils/portfolio-alerts.test.ts`, `src/lib/utils/bond-optimizer.test.ts` | `npx vitest run src/lib/utils/health-score.test.ts src/lib/utils/portfolio-alerts.test.ts src/lib/utils/bond-optimizer.test.ts` |

### Batch 5 — small maintainability cleanups with bounded blast radius

**Why first/now:** Start with canonical address/truncate/branding and lint warnings; avoid giant extraction unless time remains.

| Domain task | Priority | Files/area | Verification |
|---|---|---|---|
| `maintainability:AT-010` | P1 | `src/lib/utils/address-validation.ts`; `src/lib/hooks/use-watchlist.ts`; `src/lib/hooks/__tests__/use-watchlist.test.ts`; `src/components/shared/address-input.tsx` if it has local… | Low / small |
| `maintainability:AT-011` | P2 | `src/lib/utils/formatters.ts`; `src/components/layout/dashboard-shell.tsx`; `src/components/shared/recent-addresses.tsx`; `src/components/wallet/wallet-connect.tsx`; formatter tes… | Low / small |
| `maintainability:AT-014` | P2 | `src/app/learn/layout.tsx`; `src/app/learn/page.tsx`; `src/app/learn/[slug]/page.tsx` | Low / small |
| `maintainability:AT-001` | P0 | `eslint.config.mjs`; files surfaced by lint after change | Medium / small-to-medium |
| `maintainability:AT-002` | P0 | `src/lib/hooks/use-lp-positions.ts`; `src/lib/hooks/__tests__/use-lp-positions.test.ts`; `src/components/dashboard/lp-*` if progress prop changes | Medium / medium |
| `maintainability:AT-008` | P1 | `src/lib/api/proxy.ts` (new); `src/app/api/midgard/[...path]/route.ts`; `src/app/api/thorchain/[...path]/route.ts`; `src/app/api/coingecko/[...path]/route.ts`; route tests | Medium / medium |

### Batch 6 — docs/infra consistency safe without deploy

**Why first/now:** Docs/config hygiene that does not require external secrets; still needs full local gates before push.

| Domain task | Priority | Files/area | Verification |
|---|---|---|---|
| `infra-docs:TASK-03` | P1 | `.github/workflows/ci.yml`, `ansible-playbook.yml`, `compose.production.yml`, `DEPLOYMENT.md`, `README.md` | no |
| `infra-docs:TASK-07` | P2 | `compose.production.yml`, `.env.example`, `README.md`, `DEPLOYMENT.md`, optional `scripts/validate-compose-env.sh` | mostly |
| `infra-docs:TASK-09` | P2 | `Dockerfile`, `ansible-playbook.yml`, `compose.production.yml` | no |
| `infra-docs:TASK-10` | P2 | `.github/workflows/ci.yml`, `.github/workflows/opencode.yml` | no |
| `infra-docs:TASK-11` | P2 | `CLAUDE.md`, `AGENTS.md`, `README.md`, `DEPLOYMENT.md`, `docs/plans/2026-05-05-heimdall-next-steps.md`, `docs/plans/2026-05-24-audit-critical-fixes.md`, `docs/plans/2026-06-06-hei… | yes |

---

## 4. Consolidated unattended-safe backlog

All rows below were judged safe by the specialist reports for autonomous work without user presence. “Safe” still means: no live wallet broadcasts, no production deploy, no external secret changes, no CI/deploy success claims without exact evidence.

| Domain | Task | Priority | Exact files / area | Core work | Verification |
|---|---|---|---|---|---|
| Security/API | `T-01` | P0 | `src/lib/api/rate-limit.ts`; `src/app/api/**/route.ts`; `src/app/api/**/__tests__/*.test.ts` | Refactor `getClientIp()` to trust only platform/proxy headers configured for the deployment; use `net.isIP` or equivalent validation; add tests proving spoofed `x-forwarded-for` is ignored unless trusted. If `checkRateLimit` becomes async,… | `npm test -- src/app/api src/lib/api`; add/expect tests for spoofed headers and valid Vercel/proxy headers. |
| Security/API | `T-03` | P1 | `src/app/api/tax-report/route.ts`; `src/lib/utils/tax-export.ts`; `src/lib/utils/__tests__/tax-export.test.ts` | Move tax-report rate limiting before `request.json()`; require `Content-Type: application/json`; cap payload size if supported by runtime; catch malformed JSON as 400; add max date range validation in `parseTaxDateRange()`. | `npm test -- src/lib/utils/__tests__/tax-export.test.ts src/app/api`; add tests for malformed JSON, over-large or wrong content type, and date range boundaries. |
| Security/API | `T-04` | P1 | `src/app/api/midgard/[...path]/route.ts`; `src/app/api/thorchain/[...path]/route.ts`; `src/app/api/midgard/__tests__/route.test.ts`; `src/app/api/thorchain/__tests__/route.test.ts` | Implement per-path query validators before building upstream URLs; reject unknown params; clamp `limit`, `count`, `from`, `to`; restrict `interval` and action type values; keep existing path encoding and allowlists. | `npm test -- src/app/api/midgard src/app/api/thorchain`; add tests that valid queries pass and unknown, negative, huge, or malformed params return 400 without calling `fetch`. |
| Security/API | `T-05` | P1 | `src/app/api/address/[address]/route.ts`; `src/app/api/address/[address]/route.test.ts`; `src/app/api/pools/[pool]/route.ts` | Add strict `limit` parsing for address route; validate pool identifiers by regex and max length; return 400 before upstream or aggregation work for invalid values. | `npm test -- src/app/api/address`; add pools route tests or a new colocated test for invalid pool IDs and no upstream call. |
| Security/API | `T-06` | P1 | `src/lib/api/cors.ts`; `src/app/api/tax-report/route.ts`; `src/app/api/address/[address]/route.ts`; `src/app/api/pools/[pool]/route.ts`; related route tests | Add a shared no-store header helper for user-specific responses; apply it to tax CSV, address aggregation, pool aggregation, and their error paths where appropriate; keep proxy success cache headers unchanged. | `npm test -- src/app/api`; assert `Cache-Control: no-store, max-age=0` on tax/address/pools and existing proxy cache values remain unchanged. |
| Security/API | `T-07` | P2 | `src/lib/api/cors.ts`; `src/app/api/tax-report/route.ts`; route tests under `src/app/api/**` | Make CORS route-aware with allowed methods and optional headers; add `OPTIONS` for `/api/tax-report`; gate localhost origins to non-production or explicit config. | `npm test -- src/app/api`; add preflight tests for tax POST and production-mode tests that localhost is not allowed unless configured. |
| Security/API | `T-08` | P2 | `src/app/api/midgard/[...path]/route.ts`; `src/app/api/thorchain/[...path]/route.ts`; `src/lib/api/client.ts`; proxy route tests | Sanitize client-facing proxy errors: remove upstream base URLs and reflected rejected paths, add a generated request ID, and log full upstream details server-side. Review `client.ts` errors for accidental client exposure. | `npm test -- src/app/api/midgard src/app/api/thorchain`; add assertions that 403/502 bodies contain no upstream URL and no attacker-controlled path. |
| Financial correctness | `T-002` | P0 | `src/lib/transactions/bond.ts`, `src/components/dashboard/transaction-composer.tsx`, `src/lib/transactions/bond.test.ts` | Add `validateOperatorFeeBps`; reject `<0`, `>10000`, non-integers, and advanced fee without valid provider/explicit operator-fee mode; surface validation in composer. | Tests for `BOND:<node>`, `BOND:<node>:<provider>:0`, `10000`, `10001`, empty provider plus fee, leading zeros, whitespace. |
| Financial correctness | `T-003` | P0 | `src/lib/utils/calculations.ts`, `src/lib/utils/calculations.test.ts`, `src/lib/types/node.ts`, `src/lib/utils/yield-benchmarks.ts` | Replace heuristic `current_award < 1e7` with explicit unit handling; keep decimal APY support only for decimal fraction strings if still needed; align benchmark APY helper. | Tests for raw base units `1`, `9999999`, `10000000`, large awards, decimal `0.6334`, malformed/negative values, operator fee and provider-share interaction. |
| Financial correctness | `T-004` | P0 | `src/lib/hooks/use-historical-apy.ts`, add `src/lib/hooks/__tests__/use-historical-apy.test.ts` or pure helper test | Remove double division; extract pure `calculateHistoricalApy` helper for deterministic testing; preserve zero/empty guards. | Known-value tests: 10 RUNE/day on 1000 RUNE active bond, multiple intervals, zero totalActiveBond, nanosecond/second timestamp irrelevance. |
| Financial correctness | `T-005` | P0 | `src/lib/utils/fee-calculations.ts`, `src/lib/utils/__tests__/fee-calculations.test.ts`, `src/app/dashboard/rewards/page.tsx`, `src/app/dashboard/portfolio/page.tsx` | Treat zero APY as real; only fallback when APY is `null`/`undefined`; consider centralizing weighted APY helper used by both Portfolio and Rewards. | Cross-page helper tests for zero-yield, mixed zero/nonzero, missing APY, baseline unavailable, and all-zero portfolios. |
| Financial correctness | `T-006` | P1 | `src/components/dashboard/tax-export.tsx`, `src/app/dashboard/lp/page.tsx`, `src/lib/utils/tax-export.ts`, `src/lib/utils/__tests__/tax-export.test.ts`, `e2e/tax-export.spec.ts` | Replace/remove LP mock CSV; use `/api/tax-report` server export for LP tab or mark feature unavailable until historical LP reconstruction exists; normalize dates and amounts if retained. | Tests for 1e8 asset conversion, nanosecond `dateFirstAdded`, open-position withdrawal date, CSV escaping, current-position confidence label. |
| Financial correctness | `T-007` | P1 | `src/lib/utils/tax-export.ts`, `src/lib/api/midgard.ts`, `src/lib/utils/__tests__/tax-export.test.ts`, `src/app/api/tax-report/route.test.ts` if added | Add paginated action retrieval respecting Midgard limits; expose incomplete-history confidence; avoid assuming one 1000-row response is complete. | Mock 51, 1001, and incomplete history cases; verify FIFO consumes pre-period lots and marks low confidence if oldest lot unavailable. |
| Financial correctness | `T-008` | P1 | `src/lib/utils/tax-export.ts`, `src/lib/hooks/use-lp-positions.ts`, `src/lib/utils/calculations.ts`, `src/lib/utils/__tests__/tax-export.test.ts`, `src/lib/hooks/__tests__/use-lp-positions.… | Replace LP share/withdrawable `Number(BigInt(...))` math with BigInt fixed-point helpers; return exact base-unit strings and decimal display only at format boundary. | Edge tests above `Number.MAX_SAFE_INTEGER`, one-liquidity-unit member, huge pool depths, zero total units, unavailable THORNode fallback. |
| Financial correctness | `T-009` | P1 | `src/components/dashboard/il-calculator.tsx`, `src/lib/utils/il-calculator.ts`, `src/lib/utils/__tests__/il-calculator.test.ts`, component test if available | Make manual IL calculator reuse shared formula/sign convention; compute LP value from current HODL value; update labels to distinguish IL from fee-adjusted P/L. | Tests for no price change, 2x asset move, 0.5x move, invalid/zero inputs, USD value parity with shared utility. |
| Financial correctness | `T-010` | P2 | `src/app/dashboard/lp/page.tsx`, `src/app/dashboard/lp/page.test.tsx`, `src/components/dashboard/lp-summary-card.tsx` | Replace hard-coded `-` prefix with signed formatter; use consistent IL tone/value display across cards and page totals. | UI tests for negative IL (`-$10` once), zero IL, positive LP-vs-HODL outperformance. |
| Financial correctness | `T-011` | P1 | `src/lib/hooks/use-transaction-history.ts`, `src/lib/utils/tax-export.ts`, new `src/lib/utils/thorchain-memo.ts`, related tests | Extract shared THORChain memo parser for BOND, UNBOND, LEAVE; use it in transaction history and tax export when coin amounts are absent. | Tests for uppercase/lowercase memos, memo-only UNBOND base units, malformed memo amount, refund metadata precedence, LEAVE without amount. |
| Financial correctness | `T-012` | P2 | `src/lib/utils/formatters.ts`, `src/lib/utils/formatters.test.ts`, financial call sites touched above | Add explicit helpers `baseUnitsToRuneNumber`, `runeNumberToBaseUnits`, and signed USD/RUNE formatters to reduce accidental raw/human unit mixing. Migrate only audited financial paths first. | Edge tests for max safe integer raw strings, 8-decimal boundaries, negative display, NaN/Infinity, and no precision loss in string conversions. |
| Frontend UX/a11y/product | `TASK-FE-001` | P0 | `src/components/shared/address-input.tsx`, `e2e/homepage.spec.ts` | Add a proper address input label/help/error relationship, `aria-invalid`, and live error announcement; update homepage e2e to assert label and validation announcement. | No live QA needed; browser e2e recommended. |
| Frontend UX/a11y/product | `TASK-FE-002` | P0 | `src/components/layout/sidebar.tsx`, `src/components/layout/dashboard-shell.tsx`, `src/components/layout/sidebar.test.tsx`, `e2e/dashboard-navigation.spec.ts` | Add mobile drawer dialog semantics, close-label, Escape close, focus trap/return; ensure menu button labels and sidebar link names remain stable. | Browser QA recommended for mobile drawer. |
| Frontend UX/a11y/product | `TASK-FE-003` | P0 | `src/app/dashboard/nodes/page.tsx`, `e2e/dashboard-pages.spec.ts` or new `e2e/nodes.spec.ts` | Convert sortable headers to real buttons inside `<th>`, add `aria-sort`, preserve current sort behavior, and add a structured empty state with h1/CTA. | Browser QA recommended. |
| Frontend UX/a11y/product | `TASK-FE-004` | P0 | `src/app/dashboard/changelogs/page.tsx`, `src/app/dashboard/changelogs/page.test.tsx`, new `e2e/changelogs.spec.ts` | Label search/clear controls; add `aria-pressed` filters; add accordion `aria-expanded`/`aria-controls`; hide or unmount collapsed content to remove hidden links from tab order; derive latest release/freshness. | Browser QA recommended for keyboard. |
| Frontend UX/a11y/product | `TASK-FE-005` | P0 | `src/app/dashboard/rewards/page.tsx`, `src/components/ui/dialog.tsx` if adding primitive, `e2e/tax-export.spec.ts` | Replace hand-rolled tax modal with accessible dialog primitive or implement focus trap, Escape close, initial focus, and focus return. | Browser QA recommended. |
| Frontend UX/a11y/product | `TASK-FE-006` | P1 | `src/app/dashboard/portfolio/page.tsx`, `src/components/ui/button.tsx`, related pages using `Link><Button` | Add `asChild`/Slot support to Button or replace nested `<Link><Button>` with styled links; add collapsible ARIA for Fee Revenue, Market Overview, Heimdall’s Sight. | Browser QA helpful. |
| Frontend UX/a11y/product | `TASK-FE-007` | P1 | `src/app/dashboard/portfolio/page.tsx`, `src/components/dashboard/portfolio-summary.tsx`, `src/components/layout/dashboard-shell.tsx`, hooks if needed | Show honest source freshness and partial-data warnings for LP, market, fee revenue, Midgard/THORNode health; do not display “Live” when any required source is degraded. | No live QA; mocked browser e2e enough. |
| Frontend UX/a11y/product | `TASK-FE-008` | P1 | `src/app/dashboard/transactions/page.tsx`, `src/components/dashboard/transaction-composer.tsx`, `e2e/transactions.spec.ts` | Add `aria-pressed`/group label or tab semantics to BOND/UNBOND; add full-address `aria-label`/`title` to watchlist items; ensure copy feedback remains live. | No. |
| Frontend UX/a11y/product | `TASK-FE-009` | P1 | `src/app/dashboard/lp/page.tsx`, `src/components/dashboard/lp-summary-card.tsx`, `e2e/lp-il.spec.ts` | Encode address links, add accessible loading status, ensure current-only pricing warning appears with source/date/confidence and each unavailable metric explains why. | No. |
| Frontend UX/a11y/product | `TASK-FE-010` | P1 | `src/app/dashboard/risk/page.tsx`, risk component tests, `e2e/risk-security.spec.ts` | Add `aria-expanded`/`aria-controls` to Show Details; review icon-only/status chips for accessible text; clarify no-position states with successful lookup/source text. | No. |
| Frontend UX/a11y/product | `TASK-FE-011` | P1 | `src/app/dashboard/explorer/page.tsx`, `src/components/dashboard/node-explorer.tsx`, new or existing e2e explorer coverage | Add `aria-pressed` and sort-direction names to filters/sort; provide no-results and API-error actions; ensure node cards expose full addresses and fee/APY labels. | Browser QA helpful. |
| Frontend UX/a11y/product | `TASK-FE-012` | P0 | `src/app/learn/[slug]/page.tsx`, `src/app/learn/page.tsx`, `src/app/learn/layout.tsx`, new `e2e/learn.spec.ts` | Replace manual line-splitting renderer with sanitized markdown/MDX rendering; correct heading hierarchy, lists, and links; update legacy BondTrack copy. | No live QA. |
| Frontend UX/a11y/product | `TASK-FE-013` | P1 | `src/app/page.tsx`, `src/app/dashboard/layout.tsx`, `e2e/redirects.spec.ts`, `e2e/homepage.spec.ts` | Make home feature cards either non-interactive or real links; ensure missing-address dashboard state has h1 and CTA; add current address persistence messaging. | Browser QA helpful. |
| Frontend UX/a11y/product | `TASK-FE-014` | P0 | `e2e/homepage.spec.ts`, `e2e/dashboard-navigation.spec.ts`, new `e2e/a11y.spec.ts`, new `e2e/changelogs.spec.ts`, new `e2e/settings-notifications.spec.ts`, new `e2e/learn.spec.ts` | Update stale selectors; add Playwright accessibility smoke tests using `@axe-core/playwright`; cover keyboard paths for sidebar, changelog accordions, notifications toggles, learn links, tax modal. | Browser QA via Playwright required. |
| Frontend UX/a11y/product | `TASK-FE-015` | P2 | `src/components/ui/button.tsx`, `src/components/ui/switch.tsx`, `src/components/ui/tabs.tsx`, `src/components/shared/dashboard-card.tsx` | Audit UI primitives for focus-visible contrast, disabled state contrast, and consistent `type="button"` defaults for non-submit controls where wrappers are used. | Visual QA helpful. |
| Test suite/quality gates | `AT-01` | P0 | `vitest.config.ts`; optionally `src/test/vitest-config.test.ts` | Temporarily set `include` to an empty glob or delete/rename tests: `npm test` must fail instead of passing with no tests. | `npm test` |
| Test suite/quality gates | `AT-03` | P0 | `vitest.config.ts`, `src/setupTests.ts`, `src/test/setup.ts`, `src/test/msw/server.ts` | A hook/API test with an unhandled `fetch('/api/unknown')` should fail under Vitest. | `npm test` |
| Test suite/quality gates | `AT-05` | P1 | `playwright.config.ts` | A spec containing `test.only` should be rejected locally by the normal E2E/quality gate. | `npm run e2e` and `CI=true npm run e2e` |
| Test suite/quality gates | `AT-06` | P1 | `e2e/wallet.spec.ts` | Remove the sleeps/evaluate helpers; tests still pass and fail if Connect Wallet/options are not accessible by role/name. | `npx playwright test e2e/wallet.spec.ts` |
| Test suite/quality gates | `AT-07` | P1 | `e2e/comprehensive.spec.ts` | If app makes only one request in `retries failed requests`, the test must fail. | `npx playwright test e2e/comprehensive.spec.ts -g "retries failed requests"` |
| Test suite/quality gates | `AT-08` | P2 | `e2e/dashboard-navigation.spec.ts` | If active Risk link has no active class/`aria-current`, the active-page test must fail. | `npx playwright test e2e/dashboard-navigation.spec.ts -g "highlights active"` |
| Test suite/quality gates | `AT-09` | P2 | `e2e/homepage.spec.ts` | Replacing/removing the Shield feature icon while another SVG remains should fail. | `npx playwright test e2e/homepage.spec.ts -g "Shield"` |
| Test suite/quality gates | `AT-10` | P0 | `src/components/dashboard/transaction-composer.test.tsx` | Mutating memo generation, disabling validation, copy failure, or sign/broadcast error handling should fail. | `npx vitest run src/components/dashboard/transaction-composer.test.tsx --coverage` |
| Test suite/quality gates | `AT-11` | P1 | `src/components/dashboard/bond-simulator.test.tsx` | Changing APY math, preset values, min-bond warning, or invalid input handling should fail. | `npx vitest run src/components/dashboard/bond-simulator.test.tsx --coverage` |
| Test suite/quality gates | `AT-12` | P1 | `src/lib/hooks/__tests__/use-wallet.test.ts` | Removing Vultisig support or leaking `window.vultisig/window.thorchain` between tests should fail. | `npx vitest run src/lib/hooks/__tests__/use-wallet.test.ts` |
| Test suite/quality gates | `AT-13` | P0 | `src/app/api/address/[address]/route.test.ts` | Invalid addresses, rate-limited requests, limit >1000, non-bond actions, unsorted dates, malformed amounts, or upstream exceptions should fail their expected contract. | `npx vitest run 'src/app/api/address/[address]/route.test.ts'` |
| Test suite/quality gates | `AT-14` | P1 | `src/lib/hooks/__tests__/use-watchlist.test.ts`, `src/lib/api/__tests__/coinapi.test.ts` | `npm test` should not print stderr for expected invalid localStorage JSON or missing CoinAPI key. | `npm test 2> /tmp/heimdall-vitest-stderr.log; test ! -s /tmp/heimdall-vitest-stderr.log` |
| Test suite/quality gates | `AT-15` | P2 | `src/app/api/thorchain/__tests__/route.test.ts`, `src/app/api/midgard/__tests__/route.test.ts` | A successful proxy response missing `Cache-Control` or security headers should fail. | `npx vitest run src/app/api/thorchain/__tests__/route.test.ts src/app/api/midgard/__tests__/route.test.ts` |
| Test suite/quality gates | `AT-16` | P2 | `src/components/shared/api-health-banner.test.tsx` or extend existing API-health component tests | Removing the dismiss button, status messages, or a11y role/test ids should fail without running full E2E. | `npx vitest run src/components/shared/api-health-banner.test.tsx` |
| Test suite/quality gates | `AT-17` | P2 | `src/lib/hooks/__tests__/use-api-health.test.ts` | Three consecutive failures must mark an API down; fewer failures must not; recovery should mark healthy. | `npx vitest run src/lib/hooks/__tests__/use-api-health.test.ts` |
| Test suite/quality gates | `AT-18` | P2 | `src/lib/utils/health-score.test.ts`, `src/lib/utils/portfolio-alerts.test.ts`, `src/lib/utils/bond-optimizer.test.ts` | Mutating core risk/alert/optimizer thresholds should fail. | `npx vitest run src/lib/utils/health-score.test.ts src/lib/utils/portfolio-alerts.test.ts src/lib/utils/bond-optimizer.test.ts` |
| CI/CD deployment/docs | `TASK-03` | P1 | `.github/workflows/ci.yml`, `ansible-playbook.yml`, `compose.production.yml`, `DEPLOYMENT.md`, `README.md` | Replace 7-char deploy tags with full `sha-${GITHUB_SHA}` or a documented 12+ char minimum; include `${{ steps.build.outputs.digest }}` in the publish summary; optionally allow `IMAGE_DIGEST` deploys; update regexes and docs to match. | no |
| CI/CD deployment/docs | `TASK-04` | P1 | `.github/actions/install-deps/action.yml`, `Dockerfile`, `package.json`, `package-lock.json`, `CLAUDE.md`, `AGENTS.md` | Make Linux native optional dependencies deterministic: pin exact platform package versions matching parent packages or generate a lockfile that includes linux-x64 optional packages; remove unversioned `npm install --no-save --no-package-lo… | no |
| CI/CD deployment/docs | `TASK-05` | P1 | `Dockerfile`, `.github/workflows/ci.yml`, `.github/workflows/opencode.yml`, optional `.github/dependabot.yml` | Pin `node:22-slim` by digest; switch workflow runners from `ubuntu-latest` to a fixed supported runner; add Docker/GitHub Actions update automation; ensure docs mention why Alpine remains forbidden. | no |
| CI/CD deployment/docs | `TASK-06` | P1 | `.github/workflows/ci.yml`, `Dockerfile`, `ansible-playbook.yml`, `compose.production.yml`, `.env.example`, `README.md`, `DEPLOYMENT.md`, `CLAUDE.md` | Define the build-time/runtime env contract: pass every intended public build arg in CI, remove runtime-only `NEXT_PUBLIC_*` entries that cannot affect the client, keep server-only proxy vars in Ansible/compose, and update override docs to … | no |
| CI/CD deployment/docs | `TASK-07` | P2 | `compose.production.yml`, `.env.example`, `README.md`, `DEPLOYMENT.md`, optional `scripts/validate-compose-env.sh` | Align compose endpoints with CI and `.env.example`; add a tiny validation script or documented command that rejects non-hex `IMAGE_SHA` before `docker compose`; clarify compose is diagnostic only. | mostly |
| CI/CD deployment/docs | `TASK-09` | P2 | `Dockerfile`, `ansible-playbook.yml`, `compose.production.yml` | Standardize all container healthchecks on `http://127.0.0.1:3000/api/health` and add Node request error handlers in Dockerfile and Ansible healthcheck commands. | no |
| CI/CD deployment/docs | `TASK-10` | P2 | `.github/workflows/ci.yml`, `.github/workflows/opencode.yml` | Remove unused top-level `checks: write` or scope it to a job that proves need; add job-level `timeout-minutes` for test, build, e2e, docker-build, publish, and opencode. | no |
| CI/CD deployment/docs | `TASK-11` | P2 | `CLAUDE.md`, `AGENTS.md`, `README.md`, `DEPLOYMENT.md`, `docs/plans/2026-05-05-heimdall-next-steps.md`, `docs/plans/2026-05-24-audit-critical-fixes.md`, `docs/plans/2026-06-06-heimdall-rele… | Add `SUPERSEDED/HISTORICAL PLAN - DO NOT EXECUTE VERBATIM` banners to obsolete plans; fix CLAUDE's Docker stage description; remove `latest`, `ci-cd.yml`, `CI/CD Pipeline`, and Alpine instructions from active docs or mark them as historica… | yes |
| Maintainability/code quality | `AT-001` | P0 | `eslint.config.mjs`; files surfaced by lint after change | Change disabled React hooks rules to `warn`; run lint; fix straightforward dependency/set-state cases; leave documented TODO only if rule surfaces ambiguous behavior. | Medium / small-to-medium |
| Maintainability/code quality | `AT-002` | P0 | `src/lib/hooks/use-lp-positions.ts`; `src/lib/hooks/__tests__/use-lp-positions.test.ts`; `src/components/dashboard/lp-*` if progress prop changes | Rename SWR key to `['lp-positions', address]`; remove `setLoadingProgress` from fetcher or isolate with cancellation; keep returned shape compatible. | Medium / medium |
| Maintainability/code quality | `AT-003` | P0 | `src/lib/swr-keys.ts` (new); all files in `src/lib/hooks/*.ts`; `src/components/layout/dashboard-shell.tsx` | Add typed SWR key factory constants/predicates; update hooks to use them; update refresh to mutate all dashboard keys including tuple keys and optional-section keys. | Medium / medium |
| Maintainability/code quality | `AT-004` | P1 | `src/app/dashboard/portfolio/page.tsx` | Gate optional data hooks behind `showFeeRevenue`, `showMarket`, `showIntelligence`; avoid blocking initial render on collapsed panels; add section-level loading states. | Medium / medium |
| Maintainability/code quality | `AT-005` | P1 | `src/app/dashboard/changelogs/page.tsx`; `src/lib/utils/changelogs.ts` (new); `src/components/dashboard/changelogs/*` (new); `src/app/dashboard/changelogs/page.test.tsx` | Move pure filter/query/highlight helpers; extract toolbar/year-nav/month/entry components; keep route page as orchestration only. | Medium / medium-large |
| Maintainability/code quality | `AT-006` | P1 | `src/app/dashboard/risk/page.tsx`; `src/components/dashboard/risk-summary-banner.tsx` (new); `src/components/dashboard/risk-nodes-list.tsx` (new); `src/lib/utils/risk-display.ts` (new) | Move nested components and severity/format helpers out of page; add focused tests for severity sorting/empty state if absent. | Medium / medium |
| Maintainability/code quality | `AT-007` | P1 | `src/components/dashboard/transaction-composer.tsx`; `src/components/dashboard/transaction-composer/*` (new); `src/lib/hooks/use-transaction-composer-state.ts` (new) | Extract URL-param sync/state reducer, copy feedback hook, form fields, preview/result panel; preserve props and DOM labels. | Medium / medium-large |
| Maintainability/code quality | `AT-008` | P1 | `src/lib/api/proxy.ts` (new); `src/app/api/midgard/[...path]/route.ts`; `src/app/api/thorchain/[...path]/route.ts`; `src/app/api/coingecko/[...path]/route.ts`; route tests | Extract proxy header/rate-limit/allowlist helpers; keep per-route allowlist and Thornode path normalization intact. | Medium / medium |
| Maintainability/code quality | `AT-009` | P1 | `src/lib/api/client.ts`; `src/lib/api/coingecko.ts`; `src/lib/api/coinapi.ts`; `src/lib/api/midgard.ts`; `src/app/dashboard/rewards/page.tsx`; `src/components/dashboard/tax-export.tsx`; `sr… | Add shared fetch helpers for JSON/text/blob with timeout and consistent error messages; migrate raw fetch sites that do not require route-specific behavior. | Medium / medium |
| Maintainability/code quality | `AT-010` | P1 | `src/lib/utils/address-validation.ts`; `src/lib/hooks/use-watchlist.ts`; `src/lib/hooks/__tests__/use-watchlist.test.ts`; `src/components/shared/address-input.tsx` if it has local validation | Replace local watchlist validator with canonical utility; align test cases for mainnet/testnet/invalid addresses. | Low / small |
| Maintainability/code quality | `AT-011` | P2 | `src/lib/utils/formatters.ts`; `src/components/layout/dashboard-shell.tsx`; `src/components/shared/recent-addresses.tsx`; `src/components/wallet/wallet-connect.tsx`; formatter tests | Add shared `truncateAddress`; replace local helpers; assert default and wallet-short variants. | Low / small |
| Maintainability/code quality | `AT-012` | P2 | `src/lib/storage.ts` (new); `src/app/dashboard/layout.tsx`; `src/app/page.tsx`; `src/app/dashboard/changelogs/page.tsx`; `src/components/alerts/alert-toast.tsx`; `src/components/dashboard/p… | Add typed local/session storage helpers and storage key constants; migrate direct JSON parsing and key migration logic; keep `BONDTRACK_ADDRESS` unchanged. | Medium / medium-large |
| Maintainability/code quality | `AT-013` | P2 | `src/lib/logger.ts` (new); all files with `console.*` found by scan | Add environment-gated logger; replace console noise; keep error boundary logging if desired but route through logger. | Low / small-medium |
| Maintainability/code quality | `AT-014` | P2 | `src/app/learn/layout.tsx`; `src/app/learn/page.tsx`; `src/app/learn/[slug]/page.tsx` | Replace visible `BondTrack` copy with current Heimdall naming; do not rename `BONDTRACK_ADDRESS`. | Low / small |
| Maintainability/code quality | `AT-016` | P2 | `tsconfig.strictest.json` (new); `package.json`; files surfaced by strictest compile | Add `typecheck:strictest`; enable `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `noImplicitOverride`; initially non-CI if many findings. | Medium / medium-large |
| Maintainability/code quality | `AT-017` | P3 | `src/data/changelogs.ts`; new content files under `src/data/changelogs/`; `src/lib/hooks/use-changelogs.ts`; changelog tests | Split embedded changelog content by year/month or JSON; add typed loader/adapter preserving `ChangelogItem[]`. | Medium / medium |
| Maintainability/code quality | `AT-018` | P3 | `src/lib/utils/amounts.ts` (new); `src/lib/api/midgard.ts`; `src/lib/utils/tax-export.ts`; `src/lib/utils/calculations.ts`; `src/lib/utils/lp-analytics.ts`; related tests | Centralize base-unit parsing/conversion; migrate one domain at a time; keep `number` display conversions explicit. | Medium / medium-large |

---

## 5. Hold / needs decision, external infra, live-wallet proof, or non-prod target

Do not silently do these while the user is away unless the missing condition is explicitly resolved. Some can be partially prepared with tests/docs, but final acceptance needs user input, external settings, or non-prod infrastructure.

| Domain | Task | Priority | Why hold | Files / area |
|---|---|---|---|---|
| Security/API | `T-02` | P0 | no | `src/lib/api/rate-limit.ts`; `src/app/api/coinapi/rune-price/route.ts`; all files importing `checkRateLimit` |
| Financial correctness | `T-001` | P0 | no | `src/lib/transactions/bond.ts`, `src/lib/transactions/bond.test.ts`, optionally `src/lib/transactions/thorchain-msg-deposit.ts` |
| Test suite/quality gates | `AT-02` | P0 | no - threshold values require product/owner acceptance | `vitest.config.ts` |
| Test suite/quality gates | `AT-04` | P1 | no - long-running and may need CI workflow design approval | `package.json` plus optionally `.github/workflows/ci.yml` |
| CI/CD deployment/docs | `TASK-01` | P0 | no | `.github/workflows/opencode.yml` |
| CI/CD deployment/docs | `TASK-02` | P0 | no | `.github/workflows/ci.yml`, `ansible-playbook.yml`, `README.md`, `DEPLOYMENT.md`, `CLAUDE.md`, `AGENTS.md` |
| CI/CD deployment/docs | `TASK-08` | P2 | no | `ansible-playbook.yml`, `DEPLOYMENT.md` |
| CI/CD deployment/docs | `TASK-12` | P3 | no | `inventory/hosts.yml`, `DEPLOYMENT.md`, optional `inventory/staging.yml`, optional `inventory/production.yml` |
| Maintainability/code quality | `AT-015` | P2 | no | Candidate files from MNT-010; dependent tests/importers |

---

## 6. High-impact findings index

This section lists the highest-impact blockers first. The full specialist source reports are embedded in Appendix A.

| Domain | Finding | Severity | File/line | Evidence summary |
|---|---|---|---|---|
| Security/API | `F-01` | High | `src/lib/api/rate-limit.ts:29-56` | `getClientIp()` trusts `x-forwarded-for`, `x-real-ip`, and `cf-connecting-ip` supplied on the request; `isValidIp()` accepts loose dotted strings, so clients outside a guaranteed trusted proxy can rotate/spoof identifiers and bypass all per-IP limits. |
| Security/API | `F-02` | High | `src/lib/api/rate-limit.ts:8-21,59-101`; `src/app/api/coinapi/rune-price/route.ts:8-11` | Rate limits are stored in a process-local `Map`; serverless scaling, redeploys, or multiple Node workers reset or shard quotas. The paid/upstream CoinAPI daily budget is therefore not reliably enforced. |
| Financial correctness | `F-001` | Critical | `src/lib/transactions/bond.ts:97-124` | Keplr signing imports only `SigningStargateClient` and sends `typeUrl: '/types.MsgDeposit'` without registering THORChain `MsgDeposit` protobuf/registry support. |
| Financial correctness | `F-002` | High | `src/lib/transactions/bond.ts:295-303`, `src/components/dashboard/transaction-composer.tsx:328-330` | Advanced BOND memo accepts any digit string for operator fee and can emit `BOND:<node>::<fee>` when no provider is supplied. No 0-10000 bps bound or role warning exists. |
| Financial correctness | `F-003` | High | `src/lib/utils/calculations.ts:24-46` | `calculateAPY` treats any numeric `current_award < 1e7` as decimal annual APY. Probe: raw base-unit `9999999` is reported as `999999900%` instead of about `1%` for a 1460 RUNE bond at 146 churns/year. |
| Financial correctness | `F-004` | High | `src/lib/hooks/use-historical-apy.ts:21-32` | `runeToNumber(interval.bondingEarnings)` already converts 1e8 base units to RUNE, then line 27 divides the sum by `1e8` again. Probe: 10 RUNE daily earnings on 1000 RUNE active bond computes `0.00000365%` instead of `365%`. |
| Financial correctness | `F-005` | High | `src/lib/utils/fee-calculations.ts:90-99`, `src/app/dashboard/rewards/page.tsx:94-98`, `src/app/dashboard/portfolio/page.tsx:123-125` | Rewards page uses `calculateWeightedApy`, which falls back to network baseline when `pos.netAPY` is `0` (`pos.netAPY |
| Financial correctness | `F-007` | High | `src/lib/utils/tax-export.ts:138-151` | FIFO bond lots load `getActions(address, 1000, 'bond,unbond,leave', 'txType')` once. Existing project conventions note Midgard action limits should stay `<=50`; no pagination or completeness confidence exists. |
| Frontend UX/a11y/product | `UXA-001` | High | `src/components/shared/address-input.tsx:83` | Landing address input has placeholder text but no visible `<label>`, `id`, `aria-label`, `aria-invalid`, or `aria-describedby`; validation error at line 93 is plain text only. |
| Frontend UX/a11y/product | `UXA-005` | High | `src/components/layout/sidebar.tsx:44` | Mobile sidebar is visually a drawer, but there is no `role="dialog"`, `aria-modal`, focus trap, Escape handling, or focus return. |
| Frontend UX/a11y/product | `UXA-008` | High | `src/app/dashboard/portfolio/page.tsx:171` | Multiple `Link` components wrap `<Button>` elements (lines 171-181 and similar in other pages), creating nested interactive controls (`<a><button>`). |
| Frontend UX/a11y/product | `UXA-011` | High | `src/app/dashboard/nodes/page.tsx:45` | Sortable table headers are `<th onClick>` with `cursor-pointer`; they are not buttons, not keyboard focusable, and have no `aria-sort`. |
| Frontend UX/a11y/product | `UXA-014` | High | `src/app/dashboard/changelogs/page.tsx:366` | Changelog search input has no label; clear-search button at lines 375-380 is icon-only without an accessible name. |
| Frontend UX/a11y/product | `UXA-016` | High | `src/app/dashboard/changelogs/page.tsx:521` | Month accordion button lacks `aria-expanded`/`aria-controls`; collapsed content is only visually hidden via `max-h-0 opacity-0` at lines 563-566. |
| Frontend UX/a11y/product | `UXA-018` | High | `src/app/dashboard/rewards/page.tsx:312` | Tax export modal has `role="dialog"`/`aria-modal`, but no focus trap, initial focus, Escape close, outside-click handling, or focus return. |
| Frontend UX/a11y/product | `UXA-024` | High | `src/app/learn/[slug]/page.tsx:227` | Article renderer emits a second `<h1>` from markdown content inside a page that already has an article `<h1>` at line 214; list items at lines 232-235 are not wrapped in `<ul>`/`<ol>`. |
| Frontend UX/a11y/product | `UXA-025` | High | `src/app/learn/[slug]/page.tsx:239` | Markdown links such as `[Bonding Basics](/learn/bonding-basics)` from article content are rendered as plain paragraph text. |
| Test suite/quality gates | `TQ-01` | High | `vitest.config.ts:8` | Vitest has `passWithNoTests: true`. |
| Test suite/quality gates | `TQ-02` | High | `vitest.config.ts:11-16` | Coverage has provider/reporters/include/exclude, but no `coverage.thresholds`; `npm run test:coverage` passed while generated coverage was only 32.8% statements / 29.6% functions / 28.6% branches with 72 zero-hit files. |
| Test suite/quality gates | `TQ-03` | High | `vitest.config.ts:14` | Coverage include is limited to `src/lib/hooks/**`, `src/lib/utils/**`, and `src/components/**/*`; app routes/pages are omitted even though route tests exist. |
| CI/CD deployment/docs | `INFRA-01` | High | `.github/workflows/opencode.yml:3` | Workflow runs on public comment events, gates only on comment body at lines 11-15, grants `id-token: write` at lines 17-21, and executes `anomalyco/opencode/github@latest` with `OLLAMA_API_KEY` at lines 28-33. |
| CI/CD deployment/docs | `INFRA-02` | High | `.github/workflows/ci.yml:91` | Staging and PR refs only run non-pushing `docker-build` at lines 91-121. The only publishing job runs on `push` to `master` at lines 123-127. Ansible defaults to `sha-<local HEAD>` at `ansible-playbook.yml:7-8` and pulls that tag at lines 29-33. |
| CI/CD deployment/docs | `INFRA-05` | High | `.github/actions/install-deps/action.yml:24` | The composite action and Dockerfile install Linux native packages with unversioned `npm install --no-save --no-package-lock` at action lines 24-30 and Dockerfile lines 31-37. Runtime Docker install also redirects errors and continues with ` |
| Maintainability/code quality | `MNT-001` | High | `src/app/dashboard/changelogs/page.tsx:104` | Changelogs page is 666 lines with 181 JSX tag starts, 6 effects, 5 memos, localStorage parsing/persistence, URL syncing, filtering, highlighting, year nav, and entry rendering in one client component. |
| Maintainability/code quality | `MNT-002` | High | `src/app/dashboard/risk/page.tsx:54`, `src/app/dashboard/risk/page.tsx:191`, `src/app/dashboard/risk/page.tsx:455` | Risk page is 530 lines; it declares `RiskSummaryBanner`, `NodesList`, severity scoring, and page rendering in the route file. |
| Maintainability/code quality | `MNT-003` | High | `src/lib/api/midgard.ts:453`, `src/lib/api/midgard.ts:515`, `src/lib/api/midgard.ts:598` | `midgard.ts` is 604 lines / 22 functions and mixes earnings, network, THORName, LP member, rune price, and pool-history domains. It is also the #3 most-imported module (32 imports). |
| Maintainability/code quality | `MNT-004` | High | `src/lib/hooks/use-lp-positions.ts:129`, `src/lib/hooks/use-lp-positions.ts:151`, `src/lib/hooks/use-lp-positions.ts:166` | LP hook is 405 lines and does member, pool, rune price, per-pool THORNode LP, and per-pool historical pricing fetches in one SWR fetcher. |
| Maintainability/code quality | `MNT-005` | High | `src/lib/hooks/use-lp-positions.ts:131`, `src/lib/hooks/use-lp-positions.ts:157` | SWR fetcher calls `setLoadingProgress` while resolving pool promises. |

---

## 7. Implementation guardrails

- Use `source ~/.nvm/nvm.sh && nvm use 22` before Node/npm commands.
- Start each batch from a clean `staging` worktree; if implementing, create focused commits with descriptive messages.
- For financial changes: write RED unit tests first, avoid live wallet broadcasting, and keep transaction memos/amount units explicit in test names.
- For API changes: add route-handler tests that assert invalid inputs do **not** call upstream `fetch`.
- For a11y/UI changes: prefer role/name locators and keyboard tests over screenshots only.
- For deployment/CI changes: docs-only changes still need exact-SHA CI verification after push; never claim deploy/live without `/api/health` and browser/curl proof.
- If any task touches `.github/workflows/opencode.yml`, `publish`, GHCR, Ansible rollback, Redis/KV, live wallet signing, or inventory split, treat it as a parent-direct task and re-check assumptions before acting.

---

## Appendix A — Full specialist reports

### A.1 Security/API

Source temp report: `/tmp/heimdall-audit-security-api.md`

### Security/API audit
#### Findings

| ID | Severity | file:line | evidence | autonomous fix |
|---|---|---|---|---|
| F-01 | High | `src/lib/api/rate-limit.ts:29-56` | `getClientIp()` trusts `x-forwarded-for`, `x-real-ip`, and `cf-connecting-ip` supplied on the request; `isValidIp()` accepts loose dotted strings, so clients outside a guaranteed trusted proxy can rotate/spoof identifiers and bypass all per-IP limits. | Harden client-IP extraction: only trust configured proxy headers in production, prefer platform-provided trusted IPs, validate with a real IP parser, and regression-test spoofed headers. |
| F-02 | High | `src/lib/api/rate-limit.ts:8-21,59-101`; `src/app/api/coinapi/rune-price/route.ts:8-11` | Rate limits are stored in a process-local `Map`; serverless scaling, redeploys, or multiple Node workers reset or shard quotas. The paid/upstream CoinAPI daily budget is therefore not reliably enforced. | Move rate limiting to durable shared storage such as Redis/Vercel KV/Upstash with route and client keys; fail closed or degrade safely for paid upstreams when the limiter is unavailable. |
| F-03 | Medium | `src/app/api/tax-report/route.ts:13-39,66-75` | `/api/tax-report` parses JSON and validates fields before rate limiting. Large or malformed bodies consume work before quota accounting, and malformed JSON falls into the generic 500 path. | Apply rate limiting before body parsing, enforce content type and body size, parse JSON in a narrow try/catch, and return 400 for malformed payloads. |
| F-04 | Medium | `src/app/api/tax-report/route.ts:53-55`; `src/lib/utils/tax-export.ts:92-112,370-375` | Tax report date parsing validates format and order but has no maximum range. Requests can trigger broad Midgard price/pool-history lookups over very long periods. | Add a maximum report range, reject future or unreasonably old ranges as policy requires, and test boundary dates. |
| F-05 | Medium | `src/app/api/midgard/[...path]/route.ts:47,75`; `src/app/api/thorchain/[...path]/route.ts:72,101`; `src/lib/api/midgard.ts:423-429` | Midgard and THORNode proxies append arbitrary query strings to allowed paths. There is no per-path allowlist or cap for parameters such as `limit`, `count`, `from`, `to`, `interval`, or `txType`, which enables expensive upstream requests and unexpected query behavior through otherwise allowed paths. | Add per-route `URLSearchParams` validators, reject unknown parameters, clamp numeric ranges, and keep encoded path allowlists. |
| F-06 | Medium | `src/app/api/address/[address]/route.ts:21-23,50-52` | `/api/address/[address]` accepts `limit` via `parseInt`; `NaN`, negative values, zero, and odd strings can be forwarded to Midgard after `Math.min(limit, 1000)`. | Validate `limit` as an integer in an explicit range such as 1-1000, default only when absent, and return 400 for invalid values. |
| F-07 | Low | `src/app/api/pools/[pool]/route.ts:16-18,46-48` | `/api/pools/[pool]` does not validate pool asset format or length before doing aggregation work; malformed or very long values are accepted and compared throughout the earnings payload. | Validate pool identifiers with a THORChain asset-pair regex and max length before work starts. |
| F-08 | Medium | `src/app/api/tax-report/route.ts:58-64`; `src/app/api/address/[address]/route.ts:88-95`; `src/app/api/pools/[pool]/route.ts:61-67` | User-specific or report-like responses lack explicit `Cache-Control: no-store`. Tax CSVs and address/pool aggregations can be stored by browsers or intermediaries unless deployment defaults prevent it. | Add no-store cache headers to user-specific API responses, especially tax CSV downloads; keep public upstream price/proxy cache headers separate. |
| F-09 | Low | `src/lib/api/cors.ts:24-27`; `src/app/api/tax-report/route.ts:13` | Shared CORS headers always advertise `GET, OPTIONS`, while `/api/tax-report` is `POST` and does not define an `OPTIONS` handler. Cross-origin POST preflights will fail and route CORS policy is not method-aware. | Make `corsHeaders()` accept route-specific methods and add explicit `OPTIONS` for POST routes. |
| F-10 | Low | `src/lib/api/cors.ts:10-15,21-25` | `localhost` origins and environment-derived app URLs are accepted unconditionally, even in production. This is low risk for public data but broadens browser-readable API access from local pages. | Gate localhost origins to non-production, normalize configured origins, and consider returning no `Access-Control-Allow-Origin` for disallowed origins instead of a fixed fallback. |
| F-11 | Low | `src/app/api/midgard/[...path]/route.ts:72-115`; `src/app/api/thorchain/[...path]/route.ts:90-132` | Upstream failure responses expose `details` containing upstream base URLs and statuses; THORNode 403 also reflects the rejected path. If deployment env points at private endpoints, this leaks topology to clients. | Return generic client errors with a request ID; log upstream details server-side only; avoid reflecting rejected paths. |

#### Autonomous tasks

| Task ID | priority | exact files | steps | verification | safe unattended yes/no |
|---|---|---|---|---|---|
| T-01 | P0 | `src/lib/api/rate-limit.ts`; `src/app/api/**/route.ts`; `src/app/api/**/__tests__/*.test.ts` | Refactor `getClientIp()` to trust only platform/proxy headers configured for the deployment; use `net.isIP` or equivalent validation; add tests proving spoofed `x-forwarded-for` is ignored unless trusted. If `checkRateLimit` becomes async, update all route callers. | `npm test -- src/app/api src/lib/api`; add/expect tests for spoofed headers and valid Vercel/proxy headers. | yes |
| T-02 | P0 | `src/lib/api/rate-limit.ts`; `src/app/api/coinapi/rune-price/route.ts`; all files importing `checkRateLimit` | Replace the process-local `Map` limiter with shared Redis/Vercel KV/Upstash fixed-window or sliding-window storage; namespace keys by route and client; add limiter-unavailable behavior, failing closed for CoinAPI. | Unit-test limiter with mocked store; run `npm test -- src/app/api src/lib/api`; staging smoke test that repeated requests across two app instances share one quota. | no |
| T-03 | P1 | `src/app/api/tax-report/route.ts`; `src/lib/utils/tax-export.ts`; `src/lib/utils/__tests__/tax-export.test.ts` | Move tax-report rate limiting before `request.json()`; require `Content-Type: application/json`; cap payload size if supported by runtime; catch malformed JSON as 400; add max date range validation in `parseTaxDateRange()`. | `npm test -- src/lib/utils/__tests__/tax-export.test.ts src/app/api`; add tests for malformed JSON, over-large or wrong content type, and date range boundaries. | yes |
| T-04 | P1 | `src/app/api/midgard/[...path]/route.ts`; `src/app/api/thorchain/[...path]/route.ts`; `src/app/api/midgard/__tests__/route.test.ts`; `src/app/api/thorchain/__tests__/route.test.ts` | Implement per-path query validators before building upstream URLs; reject unknown params; clamp `limit`, `count`, `from`, `to`; restrict `interval` and action type values; keep existing path encoding and allowlists. | `npm test -- src/app/api/midgard src/app/api/thorchain`; add tests that valid queries pass and unknown, negative, huge, or malformed params return 400 without calling `fetch`. | yes |
| T-05 | P1 | `src/app/api/address/[address]/route.ts`; `src/app/api/address/[address]/route.test.ts`; `src/app/api/pools/[pool]/route.ts` | Add strict `limit` parsing for address route; validate pool identifiers by regex and max length; return 400 before upstream or aggregation work for invalid values. | `npm test -- src/app/api/address`; add pools route tests or a new colocated test for invalid pool IDs and no upstream call. | yes |
| T-06 | P1 | `src/lib/api/cors.ts`; `src/app/api/tax-report/route.ts`; `src/app/api/address/[address]/route.ts`; `src/app/api/pools/[pool]/route.ts`; related route tests | Add a shared no-store header helper for user-specific responses; apply it to tax CSV, address aggregation, pool aggregation, and their error paths where appropriate; keep proxy success cache headers unchanged. | `npm test -- src/app/api`; assert `Cache-Control: no-store, max-age=0` on tax/address/pools and existing proxy cache values remain unchanged. | yes |
| T-07 | P2 | `src/lib/api/cors.ts`; `src/app/api/tax-report/route.ts`; route tests under `src/app/api/**` | Make CORS route-aware with allowed methods and optional headers; add `OPTIONS` for `/api/tax-report`; gate localhost origins to non-production or explicit config. | `npm test -- src/app/api`; add preflight tests for tax POST and production-mode tests that localhost is not allowed unless configured. | yes |
| T-08 | P2 | `src/app/api/midgard/[...path]/route.ts`; `src/app/api/thorchain/[...path]/route.ts`; `src/lib/api/client.ts`; proxy route tests | Sanitize client-facing proxy errors: remove upstream base URLs and reflected rejected paths, add a generated request ID, and log full upstream details server-side. Review `client.ts` errors for accidental client exposure. | `npm test -- src/app/api/midgard src/app/api/thorchain`; add assertions that 403/502 bodies contain no upstream URL and no attacker-controlled path. | yes |


### A.2 Financial correctness

Source temp report: `/tmp/heimdall-audit-financial.md`

### Financial correctness audit

Scope reviewed on branch `staging` under `/Users/reidar/Projectos/Heimdall` in read-only mode. Repo source was not modified. Verification run: `npm test -- --run src/lib/transactions/bond.test.ts src/lib/utils/__tests__/tax-export.test.ts src/lib/utils/__tests__/lp-analytics.test.ts src/lib/utils/__tests__/il-calculator.test.ts src/lib/utils/calculations.test.ts src/lib/hooks/__tests__/use-lp-positions.test.ts src/lib/hooks/__tests__/use-bond-positions.test.ts` returned 7 files / 59 tests passed. Additional formula probes showed `useHistoricalApy` returns `3.65e-06%` where the same inputs should be `365%`, `calculateAPY('9999999')` returns `999999900%` where raw-base-unit interpretation is ~`1%`, and Rewards weighted APY returns `15%` where strict zero-APY weighting is `5%`.

#### Findings

| ID | Severity | file:line | evidence | financial impact | autonomous fix |
|---|---|---|---|---|---|
| F-001 | Critical | `src/lib/transactions/bond.ts:97-124` | Keplr signing imports only `SigningStargateClient` and sends `typeUrl: '/types.MsgDeposit'` without registering THORChain `MsgDeposit` protobuf/registry support. | Keplr BOND/UNBOND signing can fail before broadcast, blocking live financial operations; if a wallet signs a malformed message, users may see unexplained failed deposits. | Add a THORChain MsgDeposit registry/encoder or switch Keplr path to a wallet-supported THORChain deposit API; add unit tests that assert the registered type URL and message shape without broadcasting. |
| F-002 | High | `src/lib/transactions/bond.ts:295-303`, `src/components/dashboard/transaction-composer.tsx:328-330` | Advanced BOND memo accepts any digit string for operator fee and can emit `BOND:<node>::<fee>` when no provider is supplied. No 0-10000 bps bound or role warning exists. | Users can copy/sign memos that are rejected by THORChain or that attempt an unintended operator-fee change; a malformed memo can delay bonding and create avoidable fee/risk exposure. | Validate operator fee as integer bps `0..10000`, require an explicit provider address unless the user confirms node-operator fee update semantics, and add memo tests for empty provider, max bps, over max, and leading zeros. |
| F-003 | High | `src/lib/utils/calculations.ts:24-46` | `calculateAPY` treats any numeric `current_award < 1e7` as decimal annual APY. Probe: raw base-unit `9999999` is reported as `999999900%` instead of about `1%` for a 1460 RUNE bond at 146 churns/year. | Low-award nodes can show absurd APY/reward projections, distorting node choice, optimizer output, and portfolio reward expectations. | Make award units explicit: integer strings from THORNode are base units; only decimal strings containing `.` and `0 < value <= 1` may be interpreted as annual APY fractions. Add boundary tests for `1`, `9999999`, `10000000`, `0.6334`, malformed strings. |
| F-004 | High | `src/lib/hooks/use-historical-apy.ts:21-32` | `runeToNumber(interval.bondingEarnings)` already converts 1e8 base units to RUNE, then line 27 divides the sum by `1e8` again. Probe: 10 RUNE daily earnings on 1000 RUNE active bond computes `0.00000365%` instead of `365%`. | Historical APY used by auto-compounding is understated by 100,000,000x, making blended forecasts and “realistic mode” materially wrong. | Remove the second `/ 1e8`, rename variables to `totalBondingEarningsRune`, and add an edge test with known base-unit earnings, empty intervals, and zero active bond. |
| F-005 | High | `src/lib/utils/fee-calculations.ts:90-99`, `src/app/dashboard/rewards/page.tsx:94-98`, `src/app/dashboard/portfolio/page.tsx:123-125` | Rewards page uses `calculateWeightedApy`, which falls back to network baseline when `pos.netAPY` is `0` (`pos.netAPY || baselineApyPercent`), while Portfolio uses strict position APY. Probe: positions `(1000 RUNE, 0%)` and `(1000 RUNE, 10%)` with 20% baseline show `15%` in Rewards vs `5%` strict. | Cross-page APY/reward inconsistency; zero-award or unavailable-award nodes can receive fabricated yield in Rewards and auto-compound forecasts. | Distinguish missing APY (`null`/`undefined`) from real zero; use `pos.netAPY ?? baseline` after typing `netAPY` as nullable if needed. Add cross-page unit tests comparing Portfolio and Rewards weighted APY for zero-yield nodes. |
| F-006 | Medium | `src/components/dashboard/tax-export.tsx:48-83`, `src/app/dashboard/lp/page.tsx:155-184` | The LP tab still exposes a client-side “mock” tax export: asset deposits/withdrawals use `parseFloat` on 1e8 base-unit strings, dates assume seconds only, `Withdrawal Date` is `dateLastAdded`, and taxable event classification is explicitly mock. | The LP CSV can overstate asset quantities by 1e8, show invalid dates for nanosecond Midgard timestamps, and misclassify open positions as tax events. | Remove the legacy mock export from the LP tab or route it through `/api/tax-report`; normalize timestamps, convert all 1e8 asset/RUNE quantities with shared helpers, and label current-position estimates. |
| F-007 | High | `src/lib/utils/tax-export.ts:138-151` | FIFO bond lots load `getActions(address, 1000, 'bond,unbond,leave', 'txType')` once. Existing project conventions note Midgard action limits should stay `<=50`; no pagination or completeness confidence exists. | Active addresses with more history than the single response can miss older lots, making FIFO cost basis too low and gains too high; endpoint rejection/partial data can silently degrade tax output. | Implement paginated action loading within Midgard-supported limits, stop at earliest needed lot coverage/end timestamp, and mark reports low confidence if history is incomplete. Add >1000-action and oldest-lot tests. |
| F-008 | Medium | `src/lib/utils/tax-export.ts:292-329` | LP tax income allocation converts `liquidityUnits` with `Number(BigInt(...))`; THORChain liquidity-unit values can exceed JS safe integer precision. | User share of pool earnings can be rounded incorrectly for large pools, misallocating taxable LP income. | Use BigInt fixed-point division for `userLiquidityUnits / totalLiquidityUnits`, or decimal library math; add tests above `Number.MAX_SAFE_INTEGER` and tiny-share pools. |
| F-009 | Medium | `src/lib/hooks/use-lp-positions.ts:94-103`, `src/lib/utils/calculations.ts:201-228` | LP ownership and fallback withdrawable estimates convert BigInt units/depths to `Number` and then multiply floats before flooring back to base units. | If THORNode LP data is unavailable, current value and P/L estimates can drift for high-liquidity pools or very small shares. | Replace ownership-percent fallback with exact base-unit math: `redeem = depth * memberUnits / totalUnits` using BigInt, then format for display. Add max-depth and 1-unit share tests. |
| F-010 | Medium | `src/components/dashboard/il-calculator.tsx:43-50`, `src/lib/utils/il-calculator.ts:13-47` | Manual IL calculator uses a positive-loss sign convention and computes `lpValue = entryValue * (1 - ilPercent/100)`. The shared utility returns negative IL percent and LP value should be based on current HODL value, not original entry value. | Manual IL scenarios can materially misstate LP value and IL dollars, conflicting with position-card IL math. | Make the component call shared IL helpers or compute `lpValue = hodlValue * (2*sqrt(r)/(1+r))`; standardize sign display. Add parity tests against `calculateIL` for 2x, 0.5x, and no-change price ratios. |
| F-011 | Low | `src/app/dashboard/lp/page.tsx:136-139` | Total Impermanent Loss renders `-${formatUsd(totalStats.totalIl)}` even though IL values are already signed; negative IL can display as `--$X`. | Misleading loss presentation on LP summary totals; not a calculation error but erodes trust. | Use a signed currency formatter once and render losses/gains by sign. Add UI tests for negative, zero, and positive total IL. |
| F-012 | Medium | `src/lib/hooks/use-transaction-history.ts:62-69`, `src/lib/utils/tax-export.ts:85-90` | Transaction history extracts UNBOND amount only from coins/in/out, while tax export has a memo fallback for `UNBOND:<node>:<baseUnits>`. Composer also documents that unbond amount belongs in the memo. | Transaction history can show `0.00` for valid unbond requests when Midgard has no RUNE coin amount yet, conflicting with tax export and transaction preview. | Share a memo parser between transaction history and tax export; parse UNBOND base units when coin amount is absent. Add tests for memo-only UNBOND, LEAVE, refunds, and failed transactions. |

#### Autonomous tasks

| Task ID | priority | exact files | steps | tests/verification | safe unattended yes/no |
|---|---|---|---|---|---|
| T-001 | P0 | `src/lib/transactions/bond.ts`, `src/lib/transactions/bond.test.ts`, optionally `src/lib/transactions/thorchain-msg-deposit.ts` | Implement/encode THORChain MsgDeposit for Keplr with an explicit registry or supported wallet broadcast API; assert denom `rune`, BOND transfer amount in base units, UNBOND transfer amount zero, memo unchanged. | Unit tests with mocked Keplr signer/client; edge cases for BOND min amount, UNBOND memo-only amount, unregistered type failure path. Do not live-broadcast in CI. | no |
| T-002 | P0 | `src/lib/transactions/bond.ts`, `src/components/dashboard/transaction-composer.tsx`, `src/lib/transactions/bond.test.ts` | Add `validateOperatorFeeBps`; reject `<0`, `>10000`, non-integers, and advanced fee without valid provider/explicit operator-fee mode; surface validation in composer. | Tests for `BOND:<node>`, `BOND:<node>:<provider>:0`, `10000`, `10001`, empty provider plus fee, leading zeros, whitespace. | yes |
| T-003 | P0 | `src/lib/utils/calculations.ts`, `src/lib/utils/calculations.test.ts`, `src/lib/types/node.ts`, `src/lib/utils/yield-benchmarks.ts` | Replace heuristic `current_award < 1e7` with explicit unit handling; keep decimal APY support only for decimal fraction strings if still needed; align benchmark APY helper. | Tests for raw base units `1`, `9999999`, `10000000`, large awards, decimal `0.6334`, malformed/negative values, operator fee and provider-share interaction. | yes |
| T-004 | P0 | `src/lib/hooks/use-historical-apy.ts`, add `src/lib/hooks/__tests__/use-historical-apy.test.ts` or pure helper test | Remove double division; extract pure `calculateHistoricalApy` helper for deterministic testing; preserve zero/empty guards. | Known-value tests: 10 RUNE/day on 1000 RUNE active bond, multiple intervals, zero totalActiveBond, nanosecond/second timestamp irrelevance. | yes |
| T-005 | P0 | `src/lib/utils/fee-calculations.ts`, `src/lib/utils/__tests__/fee-calculations.test.ts`, `src/app/dashboard/rewards/page.tsx`, `src/app/dashboard/portfolio/page.tsx` | Treat zero APY as real; only fallback when APY is `null`/`undefined`; consider centralizing weighted APY helper used by both Portfolio and Rewards. | Cross-page helper tests for zero-yield, mixed zero/nonzero, missing APY, baseline unavailable, and all-zero portfolios. | yes |
| T-006 | P1 | `src/components/dashboard/tax-export.tsx`, `src/app/dashboard/lp/page.tsx`, `src/lib/utils/tax-export.ts`, `src/lib/utils/__tests__/tax-export.test.ts`, `e2e/tax-export.spec.ts` | Replace/remove LP mock CSV; use `/api/tax-report` server export for LP tab or mark feature unavailable until historical LP reconstruction exists; normalize dates and amounts if retained. | Tests for 1e8 asset conversion, nanosecond `dateFirstAdded`, open-position withdrawal date, CSV escaping, current-position confidence label. | yes |
| T-007 | P1 | `src/lib/utils/tax-export.ts`, `src/lib/api/midgard.ts`, `src/lib/utils/__tests__/tax-export.test.ts`, `src/app/api/tax-report/route.test.ts` if added | Add paginated action retrieval respecting Midgard limits; expose incomplete-history confidence; avoid assuming one 1000-row response is complete. | Mock 51, 1001, and incomplete history cases; verify FIFO consumes pre-period lots and marks low confidence if oldest lot unavailable. | yes |
| T-008 | P1 | `src/lib/utils/tax-export.ts`, `src/lib/hooks/use-lp-positions.ts`, `src/lib/utils/calculations.ts`, `src/lib/utils/__tests__/tax-export.test.ts`, `src/lib/hooks/__tests__/use-lp-positions.test.ts` | Replace LP share/withdrawable `Number(BigInt(...))` math with BigInt fixed-point helpers; return exact base-unit strings and decimal display only at format boundary. | Edge tests above `Number.MAX_SAFE_INTEGER`, one-liquidity-unit member, huge pool depths, zero total units, unavailable THORNode fallback. | yes |
| T-009 | P1 | `src/components/dashboard/il-calculator.tsx`, `src/lib/utils/il-calculator.ts`, `src/lib/utils/__tests__/il-calculator.test.ts`, component test if available | Make manual IL calculator reuse shared formula/sign convention; compute LP value from current HODL value; update labels to distinguish IL from fee-adjusted P/L. | Tests for no price change, 2x asset move, 0.5x move, invalid/zero inputs, USD value parity with shared utility. | yes |
| T-010 | P2 | `src/app/dashboard/lp/page.tsx`, `src/app/dashboard/lp/page.test.tsx`, `src/components/dashboard/lp-summary-card.tsx` | Replace hard-coded `-` prefix with signed formatter; use consistent IL tone/value display across cards and page totals. | UI tests for negative IL (`-$10` once), zero IL, positive LP-vs-HODL outperformance. | yes |
| T-011 | P1 | `src/lib/hooks/use-transaction-history.ts`, `src/lib/utils/tax-export.ts`, new `src/lib/utils/thorchain-memo.ts`, related tests | Extract shared THORChain memo parser for BOND, UNBOND, LEAVE; use it in transaction history and tax export when coin amounts are absent. | Tests for uppercase/lowercase memos, memo-only UNBOND base units, malformed memo amount, refund metadata precedence, LEAVE without amount. | yes |
| T-012 | P2 | `src/lib/utils/formatters.ts`, `src/lib/utils/formatters.test.ts`, financial call sites touched above | Add explicit helpers `baseUnitsToRuneNumber`, `runeNumberToBaseUnits`, and signed USD/RUNE formatters to reduce accidental raw/human unit mixing. Migrate only audited financial paths first. | Edge tests for max safe integer raw strings, 8-decimal boundaries, negative display, NaN/Infinity, and no precision loss in string conversions. | yes |


### A.3 Frontend UX/a11y/product

Source temp report: `/tmp/heimdall-audit-frontend-ux.md`

### Frontend UX/a11y/product audit

Audit target: `/Users/reidar/Projectos/Heimdall` on branch `staging`. Repository source was inspected read-only. Scoped files reviewed include `src/app/dashboard/**`, `src/app/page.tsx`, `src/app/learn/**`, `src/app/dashboard/settings/**`, `src/components/layout/*`, `src/components/shared/*`, `src/components/ui/*`, and wallet/transaction components. The requested path `src/components/dashboard/layout/wallet/shared/ui` does not exist in this repo; the matching actual locations are `src/components/layout`, `src/components/wallet`, `src/components/shared`, and `src/components/ui`.

#### Findings

| ID | Severity | file:line | evidence | user impact | autonomous fix |
|---|---|---|---|---|---|
| UXA-001 | High | `src/components/shared/address-input.tsx:83` | Landing address input has placeholder text but no visible `<label>`, `id`, `aria-label`, `aria-invalid`, or `aria-describedby`; validation error at line 93 is plain text only. | Screen reader users get an unlabeled edit field and may not hear why lookup failed. Voice control users cannot reliably target it. | Add `id="address-input"`, a visible or `sr-only` `<label>`, `aria-invalid={!!error}`, `aria-describedby` to an error/help element, and `role="alert"` or `aria-live="polite"` for validation messages. |
| UXA-002 | Medium | `src/app/page.tsx:124` | `FeatureCard` is a `<div>` with `cursor-pointer`, hover scale, and no click handler or keyboard semantics. | Cards look tappable but do nothing; keyboard users cannot discover or activate the implied action. | Either remove `cursor-pointer`/interactive hover affordance or convert cards to real `<Link>`/`button` targets with accessible names. |
| UXA-003 | Medium | `src/app/dashboard/layout.tsx:87` | Missing-address dashboard state renders `<h2>Enter an address to get started</h2>` but no page-level `<h1>`. | Heading outline starts at level 2 on an important state; screen reader page navigation is degraded. | Change the missing-address heading to `<h1>` or add an offscreen `<h1>` before the state card. |
| UXA-004 | Medium | `src/components/layout/sidebar.tsx:53` | Mobile drawer close control is an icon-only `<button>` without `aria-label`; overlay at lines 37-41 is clickable but not described. | Screen reader users hear an unlabeled button and cannot understand drawer dismissal. | Add `aria-label="Close menu"` to the close button and mark the backdrop `aria-hidden="true"`; optionally close on Escape and return focus to the menu button. |
| UXA-005 | High | `src/components/layout/sidebar.tsx:44` | Mobile sidebar is visually a drawer, but there is no `role="dialog"`, `aria-modal`, focus trap, Escape handling, or focus return. | Keyboard users can tab behind the open drawer and may lose context on mobile. | Treat mobile sidebar as a modal dialog: focus first actionable item on open, trap focus, close on Escape/backdrop, and restore focus to `MobileMenuButton`. |
| UXA-006 | Medium | `src/components/layout/dashboard-shell.tsx:164` | Source freshness pill is hidden on small screens and initializes from local tick state as `0s ago`; refresh only mutates selected SWR keys at lines 20-29. | Mobile users lose the only visible data freshness clue; `0s ago` can imply live data even before all requests succeed. | Show a compact mobile freshness/status label, tie timestamps to actual successful fetch completion where possible, and expose source names (`Midgard`, `THORNode`) in an accessible tooltip/text. |
| UXA-007 | Medium | `src/app/dashboard/portfolio/page.tsx:183` | Portfolio shows a generic “Live” badge without source, last updated time, or degraded state; fee/market panels are collapsed behind unlabeled data recency. | Financial users may overtrust stale or partial data. | Replace with “Updated X ago from Midgard/THORNode” and degrade badge text when any upstream hook errors or has not loaded. |
| UXA-008 | High | `src/app/dashboard/portfolio/page.tsx:171` | Multiple `Link` components wrap `<Button>` elements (lines 171-181 and similar in other pages), creating nested interactive controls (`<a><button>`). | Invalid HTML can produce duplicate focus stops and inconsistent activation for assistive tech. | Use a link styled with `buttonVariants`, add an `asChild` pattern to `Button`, or render `<Button>` as a child of Radix Slot. |
| UXA-009 | Medium | `src/app/dashboard/portfolio/page.tsx:288` | Collapsible buttons for Fee Revenue, Market Overview, and Heimdall’s Sight lack `type="button"`, `aria-expanded`, and `aria-controls`; the Heimdall’s Sight chevron button at line 354 is icon-only with no label. | Screen reader users cannot know whether sections are expanded; icon-only control is unnamed. | Add `type="button"`, stable panel ids, `aria-expanded`, `aria-controls`, and `aria-label` for icon-only toggles. |
| UXA-010 | Medium | `src/app/dashboard/portfolio/page.tsx:114` | LP errors are detected (`lpDataUnavailable`) and positions are zeroed, but no warning is rendered on Portfolio. | A user can see $0 LP allocation and infer they have no LP exposure when the LP source actually failed. | Render a non-blocking warning in Asset Allocation and summary cards when LP data is unavailable, linking to `/dashboard/lp` retry/details. |
| UXA-011 | High | `src/app/dashboard/nodes/page.tsx:45` | Sortable table headers are `<th onClick>` with `cursor-pointer`; they are not buttons, not keyboard focusable, and have no `aria-sort`. | Keyboard and screen reader users cannot sort or discover the current sort direction. | Put a `<button type="button">` inside each `<th>`, add `aria-sort` to sorted column headers, and support Enter/Space via native button semantics. |
| UXA-012 | Medium | `src/app/dashboard/nodes/page.tsx:109` | Empty Nodes page returns only `<p>No bonded positions found.</p>` before rendering the page `<h1>` and without a next action. | Empty state lacks page title, source/trust explanation, and a way to bond or switch address. | Render normal page shell first with `<h1>Nodes</h1>`, then an empty card explaining successful lookup/no current bond and CTA to Transactions/Explorer. |
| UXA-013 | Medium | `src/app/dashboard/nodes/page.tsx:137` | Wide node comparison table is wrapped in `overflow-hidden`, not horizontal scrolling, despite seven columns. | On mobile, columns can clip or become unreadably cramped. | Change wrapper to `overflow-x-auto`, add `min-w-[...]`, preserve sticky header, and test narrow viewport. |
| UXA-014 | High | `src/app/dashboard/changelogs/page.tsx:366` | Changelog search input has no label; clear-search button at lines 375-380 is icon-only without an accessible name. | Search and clear controls are ambiguous to assistive tech. | Add `id`/`label` or `aria-label="Search changelogs"`; add `aria-label="Clear changelog search"` and `type="button"` to the clear button. |
| UXA-015 | Medium | `src/app/dashboard/changelogs/page.tsx:390` | Type filter and year quick-nav buttons do not expose selected state (`aria-pressed`) or current year/section. | Screen reader users cannot tell which filter is active. | Add `aria-pressed={isActive}` to filter buttons and label year jump buttons (`Jump to 2024 changelogs`). |
| UXA-016 | High | `src/app/dashboard/changelogs/page.tsx:521` | Month accordion button lacks `aria-expanded`/`aria-controls`; collapsed content is only visually hidden via `max-h-0 opacity-0` at lines 563-566. | Assistive tech may not understand accordion state; hidden links inside collapsed panels can remain in the tab order. | Add accordion ARIA and either conditionally render collapsed content, set `hidden`, or use `inert`/focus management while collapsed. |
| UXA-017 | Medium | `src/app/dashboard/changelogs/page.tsx:351` | Protocol statistics hard-code `Latest Release v3.16`; source footer only names Medium at lines 651-661 and does not show last fetched/updated timestamp. | Users may trust stale release information as current protocol state. | Derive latest release from changelog data or protocol hook, show “last updated/fetched” date, and label Medium as a community source. |
| UXA-018 | High | `src/app/dashboard/rewards/page.tsx:312` | Tax export modal has `role="dialog"`/`aria-modal`, but no focus trap, initial focus, Escape close, outside-click handling, or focus return. | Keyboard users can tab behind the modal or lose their prior context after closing. | Use a dialog primitive (Radix Dialog) or implement focus trap, Escape close, initial focus on Start Date, and restore focus to Export Tax Report. |
| UXA-019 | Medium | `src/app/dashboard/risk/page.tsx:475` | “Show Details” toggle lacks `type="button"`, `aria-expanded`, and `aria-controls`. | Users cannot determine whether detailed risk panels are open. | Add `type="button"`, an id for the details grid, and `aria-expanded={showDetails}`/`aria-controls`. |
| UXA-020 | Medium | `src/app/dashboard/transactions/page.tsx:66` | Watchlist address buttons show only `addr.slice(0, 10)}...` and have no `aria-label`/`title` with the full address. | Users cannot distinguish saved addresses that share the same prefix; screen reader users only hear a truncated value. | Add `aria-label={`Open dashboard for ${addr}`}` and visible middle+end truncation; include `title={addr}`. |
| UXA-021 | Medium | `src/components/dashboard/transaction-composer.tsx:247` | BOND/UNBOND segmented controls are plain buttons with visual selected state only; no `aria-pressed`, `role="tablist"`, or radio semantics. | Assistive tech users do not know which transaction mode is active. | Implement as `role="tablist"`/`role="tab"` or add `aria-pressed={mode === ...}` and group label. |
| UXA-022 | Medium | `src/app/dashboard/lp/page.tsx:49` | Back-to-dashboard link interpolates raw `address` instead of `encodeURIComponent(address)`; loading spinner at line 165 has no `role="status"` or accessible text bound to the spinner. | Special characters in future address aliases can break navigation; loading state is less discoverable to screen readers. | Encode the address in links and add `role="status" aria-live="polite"` to loading states. |
| UXA-023 | Medium | `src/app/dashboard/explorer/page.tsx:173` | Fee filter/sort buttons indicate active state only through color; no `aria-pressed`, no `aria-sort`, and sorting has no announced direction beyond an icon. | Low-vision and screen reader users cannot reliably understand active filters or sort direction. | Add `aria-pressed` to filters and sort controls; expose `aria-label="Sort by APY, descending"`; add visible text for direction. |
| UXA-024 | High | `src/app/learn/[slug]/page.tsx:227` | Article renderer emits a second `<h1>` from markdown content inside a page that already has an article `<h1>` at line 214; list items at lines 232-235 are not wrapped in `<ul>`/`<ol>`. | Heading and list semantics are invalid/noisy for screen readers and document outline. | Use a markdown renderer/MDX pipeline or map content heading level 1 to `<h2>` inside the article body and wrap contiguous list items in proper lists. |
| UXA-025 | High | `src/app/learn/[slug]/page.tsx:239` | Markdown links such as `[Bonding Basics](/learn/bonding-basics)` from article content are rendered as plain paragraph text. | “Next Steps” links in educational content are not clickable, blocking learning/navigation paths. | Render markdown links to Next `<Link>`/`<a>` elements via a markdown parser with a sanitized component map. |
| UXA-026 | Medium | `src/app/learn/[slug]/page.tsx:153` | Health Score article says “How BondTrack calculates…” while the app shell and landing brand are Heimdall. | Brand inconsistency reduces trust, especially in educational/security content. | Replace legacy BondTrack mentions and metadata with Heimdall unless intentionally documenting legacy naming. |
| UXA-027 | Medium | `e2e/homepage.spec.ts:18` | Homepage e2e expects “Real-time Monitoring” and “Rewards Tracking”, but current page cards are “Node Health”, “Earnings”, “Risk Alerts”, and “Transactions” at `src/app/page.tsx:65-88`. | CI may fail or the test may no longer cover the actual landing UX. | Update homepage e2e selectors to current copy and add semantic assertions for address label/error. |
| UXA-028 | Medium | `e2e/dashboard-navigation.spec.ts:16` | Sidebar links use `aria-label="Navigate to Portfolio page"` at `src/components/layout/sidebar.tsx:95`; e2e searches links by name “Portfolio”. | Tests may be brittle/failing because the accessible name is the aria-label, not visible text. | Either remove redundant aria-labels so visible text is the accessible name, or update tests to the actual accessible names; prefer visible text names. |
| UXA-029 | Medium | `e2e/*.spec.ts` | Static scan found 12 Playwright specs; none mention `settings`, `notifications`, `learn`, `axe`, `aria`, or `keyboard`; only `changelogs` appears in navigation smoke coverage. | Known high-risk a11y and product states are unprotected by end-to-end tests. | Add e2e coverage for changelog search/accordion keyboard flows, notification preferences, learn article links/outline, and an axe smoke pass on critical routes. |

#### Autonomous tasks

| Task ID | priority | exact files | steps | tests/verification | needs browser/live QA? | safe unattended yes/no |
|---|---|---|---|---|---|---|
| TASK-FE-001 | P0 | `src/components/shared/address-input.tsx`, `e2e/homepage.spec.ts` | Add a proper address input label/help/error relationship, `aria-invalid`, and live error announcement; update homepage e2e to assert label and validation announcement. | `npm run test -- address-input` if available; `npm run e2e -- homepage.spec.ts`; manual screen-reader smoke optional. | No live QA needed; browser e2e recommended. | yes |
| TASK-FE-002 | P0 | `src/components/layout/sidebar.tsx`, `src/components/layout/dashboard-shell.tsx`, `src/components/layout/sidebar.test.tsx`, `e2e/dashboard-navigation.spec.ts` | Add mobile drawer dialog semantics, close-label, Escape close, focus trap/return; ensure menu button labels and sidebar link names remain stable. | Unit test focus/open/close behavior; Playwright mobile viewport nav test; keyboard-only tab order check. | Browser QA recommended for mobile drawer. | yes |
| TASK-FE-003 | P0 | `src/app/dashboard/nodes/page.tsx`, `e2e/dashboard-pages.spec.ts` or new `e2e/nodes.spec.ts` | Convert sortable headers to real buttons inside `<th>`, add `aria-sort`, preserve current sort behavior, and add a structured empty state with h1/CTA. | Playwright: sort via keyboard Enter/Space and assert URL/page unchanged plus sorted rows; mobile viewport table scroll assertion. | Browser QA recommended. | yes |
| TASK-FE-004 | P0 | `src/app/dashboard/changelogs/page.tsx`, `src/app/dashboard/changelogs/page.test.tsx`, new `e2e/changelogs.spec.ts` | Label search/clear controls; add `aria-pressed` filters; add accordion `aria-expanded`/`aria-controls`; hide or unmount collapsed content to remove hidden links from tab order; derive latest release/freshness. | Unit tests for `buildChangelogQuery`/filtering; Playwright keyboard test for `/`, Escape, filters, accordion tab order. | Browser QA recommended for keyboard. | yes |
| TASK-FE-005 | P0 | `src/app/dashboard/rewards/page.tsx`, `src/components/ui/dialog.tsx` if adding primitive, `e2e/tax-export.spec.ts` | Replace hand-rolled tax modal with accessible dialog primitive or implement focus trap, Escape close, initial focus, and focus return. | Playwright keyboard test: open modal, Tab cycles inside, Escape closes, focus returns to export button. | Browser QA recommended. | yes |
| TASK-FE-006 | P1 | `src/app/dashboard/portfolio/page.tsx`, `src/components/ui/button.tsx`, related pages using `Link><Button` | Add `asChild`/Slot support to Button or replace nested `<Link><Button>` with styled links; add collapsible ARIA for Fee Revenue, Market Overview, Heimdall’s Sight. | Typecheck; Playwright tab-order/role assertions for Portfolio quick actions and collapsibles. | Browser QA helpful. | yes |
| TASK-FE-007 | P1 | `src/app/dashboard/portfolio/page.tsx`, `src/components/dashboard/portfolio-summary.tsx`, `src/components/layout/dashboard-shell.tsx`, hooks if needed | Show honest source freshness and partial-data warnings for LP, market, fee revenue, Midgard/THORNode health; do not display “Live” when any required source is degraded. | Mock hook/API failures in unit/e2e; assert warning banner and no false zero LP allocation. | No live QA; mocked browser e2e enough. | yes |
| TASK-FE-008 | P1 | `src/app/dashboard/transactions/page.tsx`, `src/components/dashboard/transaction-composer.tsx`, `e2e/transactions.spec.ts` | Add `aria-pressed`/group label or tab semantics to BOND/UNBOND; add full-address `aria-label`/`title` to watchlist items; ensure copy feedback remains live. | Existing transactions e2e plus accessibility assertions for selected mode and full address labels. | No. | yes |
| TASK-FE-009 | P1 | `src/app/dashboard/lp/page.tsx`, `src/components/dashboard/lp-summary-card.tsx`, `e2e/lp-il.spec.ts` | Encode address links, add accessible loading status, ensure current-only pricing warning appears with source/date/confidence and each unavailable metric explains why. | Mock current-only LP data and assert warning, unavailable metrics, retry/error states. | No. | yes |
| TASK-FE-010 | P1 | `src/app/dashboard/risk/page.tsx`, risk component tests, `e2e/risk-security.spec.ts` | Add `aria-expanded`/`aria-controls` to Show Details; review icon-only/status chips for accessible text; clarify no-position states with successful lookup/source text. | Playwright keyboard toggle test; unit snapshots for no-position and risk status text. | No. | yes |
| TASK-FE-011 | P1 | `src/app/dashboard/explorer/page.tsx`, `src/components/dashboard/node-explorer.tsx`, new or existing e2e explorer coverage | Add `aria-pressed` and sort-direction names to filters/sort; provide no-results and API-error actions; ensure node cards expose full addresses and fee/APY labels. | E2E keyboard filter/sort/no-results tests with mocked node API. | Browser QA helpful. | yes |
| TASK-FE-012 | P0 | `src/app/learn/[slug]/page.tsx`, `src/app/learn/page.tsx`, `src/app/learn/layout.tsx`, new `e2e/learn.spec.ts` | Replace manual line-splitting renderer with sanitized markdown/MDX rendering; correct heading hierarchy, lists, and links; update legacy BondTrack copy. | Unit/render tests for heading count, list semantics, link hrefs; Playwright navigate through Learn and article links. | No live QA. | yes |
| TASK-FE-013 | P1 | `src/app/page.tsx`, `src/app/dashboard/layout.tsx`, `e2e/redirects.spec.ts`, `e2e/homepage.spec.ts` | Make home feature cards either non-interactive or real links; ensure missing-address dashboard state has h1 and CTA; add current address persistence messaging. | Homepage e2e for feature cards and address flow; redirects e2e with/without localStorage. | Browser QA helpful. | yes |
| TASK-FE-014 | P0 | `e2e/homepage.spec.ts`, `e2e/dashboard-navigation.spec.ts`, new `e2e/a11y.spec.ts`, new `e2e/changelogs.spec.ts`, new `e2e/settings-notifications.spec.ts`, new `e2e/learn.spec.ts` | Update stale selectors; add Playwright accessibility smoke tests using `@axe-core/playwright`; cover keyboard paths for sidebar, changelog accordions, notifications toggles, learn links, tax modal. | `npm run e2e`; include axe assertions with documented exceptions only. | Browser QA via Playwright required. | yes |
| TASK-FE-015 | P2 | `src/components/ui/button.tsx`, `src/components/ui/switch.tsx`, `src/components/ui/tabs.tsx`, `src/components/shared/dashboard-card.tsx` | Audit UI primitives for focus-visible contrast, disabled state contrast, and consistent `type="button"` defaults for non-submit controls where wrappers are used. | Component unit tests plus visual snapshot or Storybook/Playwright screenshots if available. | Visual QA helpful. | yes |


### A.4 Test suite/quality gates

Source temp report: `/tmp/heimdall-audit-tests.md`

### Test suite/quality gate audit

Audit baseline (staging `d92f2eb`): `npm test` passed 39 Vitest files / 190 tests in 15.27s; `npm run test:coverage` passed but emitted no coverage table; generated coverage JSON showed 108 instrumented files, 32.8% statements, 29.6% functions, 28.6% branches, and 72 zero-statement-hit files; `npm run lint` passed; `npm run build` passed; `npm run e2e` passed 62 Playwright tests in 40.9s; `npm audit --omit=dev --audit-level=moderate` found 0 production vulnerabilities. Repository source was not modified.

#### Findings

| ID | Severity | file:line | evidence | risk | autonomous fix |
|---|---|---:|---|---|---|
| TQ-01 | High | `vitest.config.ts:8` | Vitest has `passWithNoTests: true`. | A broken include glob, renamed tests, or deleted tests can still produce a green unit-test gate. | Set `passWithNoTests: false`; add a small config regression or CI smoke that fails if `vitest --list` returns zero files. |
| TQ-02 | High | `vitest.config.ts:11-16` | Coverage has provider/reporters/include/exclude, but no `coverage.thresholds`; `npm run test:coverage` passed while generated coverage was only 32.8% statements / 29.6% functions / 28.6% branches with 72 zero-hit files. | The coverage gate is observational only; large untested areas can merge without failing CI. | Add realistic global thresholds now, ratchet upward, and add critical per-file thresholds for API proxies, transaction composer, tax export, and wallet hooks. |
| TQ-03 | High | `vitest.config.ts:14` | Coverage include is limited to `src/lib/hooks/**`, `src/lib/utils/**`, and `src/components/**/*`; app routes/pages are omitted even though route tests exist. | `/api/*` proxy security, rate limits, dashboard route behavior, and Next app invariants are not measured by coverage. | Expand coverage include to `src/app/**/*.{ts,tsx}` or at least `src/app/api/**/*.{ts,tsx}` plus dashboard pages; exclude only layouts/generated/test files deliberately. |
| TQ-04 | Medium | `src/setupTests.ts:1` | Config wires only `./src/setupTests.ts`, which imports jest-dom only; `src/test/setup.ts` has localStorage/Notification/matchMedia/SWR cache setup but is unused. | Unit tests duplicate environment mocks and can leak SWR/localStorage state differently from intended shared setup. | Import `./test/setup` from `src/setupTests.ts` or point `setupFiles` at both files; then remove duplicated per-test setup. |
| TQ-05 | Medium | `src/test/msw/server.ts:3` | MSW server is `setupServer()` with no handlers and no `server.listen/resetHandlers/close` references in the repo; `src/test/msw/handlers/index.ts:4` exports handlers but they are unused. | Unit tests rely mostly on `vi.mock`/inline data, so accidental real fetches or unmocked endpoints are not caught consistently. | Initialize `setupServer(...handlers)` and wire `beforeAll/afterEach/afterAll` with `onUnhandledRequest: 'error'`; migrate API/hook tests to MSW where practical. |
| TQ-06 | Medium | `package.json:15-20` | Local scripts expose `lint`, `test`, `test:coverage`, and `e2e` separately; CI additionally runs `npm audit --omit=dev --audit-level=moderate`, `npm run lint -- --max-warnings=0`, build, e2e, and Docker build in `.github/workflows/ci.yml:33-106`. | Developers can run all package scripts locally and still miss the stricter CI gate. | Add a `quality:ci` script mirroring non-Docker CI (`npm audit ... && npm run lint -- --max-warnings=0 && npm test && npm run test:coverage && npm run build && npm run e2e`) and consider making `lint` strict by default. |
| TQ-07 | Medium | `playwright.config.ts:6-9` | Playwright forbids `.only` only under CI and uses `reporter: 'html'` only. | A local `.only` can survive until CI; CI lacks a machine-readable reporter/JUnit summary for trend/flake tracking. | Set `forbidOnly: true` or add a pre-commit/quality script with `--forbid-only`; configure reporters such as `[['list'], ['html'], ['junit', { outputFile: 'test-results/playwright-junit.xml' }]]`. |
| TQ-08 | Medium | `playwright.config.ts:21-25` | E2E always builds and manually copies standalone assets in `webServer.command`, with `reuseExistingServer: !process.env.CI`. | Local runs can exercise a stale already-running server while CI always exercises a fresh production build; command duplication is brittle. | Add an explicit `e2e:ci` script used by CI and local release checks with `CI=true`; extract standalone start into a reusable script and document/verify no stale-server mode for release gates. |
| TQ-09 | Medium | `e2e/wallet.spec.ts:6-27` | Wallet helpers use `waitForTimeout`, `page.evaluate`, `document.querySelectorAll('button')`, and text-content searches; there are 12 timeout sleeps in this file. | Wallet E2E can pass/fail based on hydration timing and bypasses user-facing accessible locators. | Replace helpers with locator-first actions (`getByRole('button', { name: ... })`, `expect(...).toBeEnabled()`), and assert wallet menu/status through roles/test IDs instead of DOM scanning. |
| TQ-10 | Medium | `e2e/comprehensive.spec.ts:72-95` | Test named `retries failed requests` waits 5 seconds and asserts only `requestCount >= 1`; it does not prove a retry occurred or that the success response was consumed. | Retry regressions can pass as long as one request is made. | Make the RED condition `requestCount === 3` or assert two 503 responses followed by a 200-backed UI state; replace the fixed sleep with `expect.poll`/`waitForResponse`. |
| TQ-11 | Medium | `e2e/dashboard-navigation.spec.ts:50-55` | `highlights active page in sidebar` clicks Risk, checks URL, then only asserts the Risk link is visible. | Active-state styling/ARIA regressions are not caught. | Assert `aria-current`, selected class, or an accessible current-page marker after navigation. |
| TQ-12 | Medium | `e2e/homepage.spec.ts:42-44` | `displays Shield icon` asserts `page.locator('svg').first()` is visible. | Any first SVG satisfies the test; the intended icon can disappear or change semantics without failure. | Prefer an accessible label/test id on the feature icon or assert the feature card content/role rather than a raw SVG locator. |
| TQ-13 | Medium | `src/components/dashboard/transaction-composer.tsx:45` | Coverage JSON reports `src/components/dashboard/transaction-composer.tsx` at 0% statements/functions/branches, while E2E covers only selected memo/copy flows. | Validation, disabled submit states, preview/sign/broadcast errors, query-param initialization, copy failure, and network-mismatch UX can regress without unit coverage. | Add component tests mocking `useWallet`, `useSearchParams`, clipboard, and transaction executors for BOND/UNBOND validation, copy success/failure, preview gating, and sign/broadcast outcomes. |
| TQ-14 | Medium | `src/components/dashboard/bond-simulator.tsx:62` | `simulateBond` and simulator state are unexported and the file had 0% coverage; the preset union includes `agressive` typo at `bond-simulator.tsx:20`/`:51`. | Reward math, minimum-bond warning, preset behavior, and typo-driven UI paths can regress silently. | Either extract pure simulator math for unit tests or render the component and test default calculation, preset buttons, invalid inputs, and minimum-bond messaging. |
| TQ-15 | Medium | `src/lib/hooks/use-wallet.ts:72` | Hook supports Vultisig detection, but `src/lib/hooks/__tests__/use-wallet.test.ts:11-14` cleanup deletes only `keplr` and `xfi`, and unit tests do not cover Vultisig. | Vultisig regressions rely on brittle E2E only; future unit tests can leak `window.vultisig`/`window.thorchain`. | Add Vultisig detection/connect/disconnect unit tests and cleanup `window.vultisig` and `window.thorchain` in `afterEach`. |
| TQ-16 | Medium | `src/app/api/address/[address]/route.test.ts:36-71` | Address route has one regression for amount labels only; source also validates address, rate-limits, caps `limit` at 1000, filters BOND/UNBOND memos, parses BigInt dates, sorts desc, and handles malformed amounts at `route.ts:23-86`. | API contract/security regressions can land around the recent tax/export amount fix. | Add focused route tests for invalid address, rate-limit headers, `limit` cap, non-bond filtering, descending timestamp sort, malformed amount fallback, and 500 error path. |
| TQ-17 | Low | `src/lib/hooks/use-watchlist.ts:35` | `npm test` emits stderr stack trace for expected invalid JSON test in `use-watchlist.test.ts:59-66`. | Expected negative-path tests make CI logs noisy and can hide real errors. | Spy on `console.error` in the test and assert it was called once, or inject a logger and silence expected errors. |
| TQ-18 | Low | `src/lib/api/coinapi.ts:44` | `npm test` emits stderr for expected missing `COINAPI_KEY` fallback in `coinapi.test.ts:16-20`. | Expected fallback behavior produces error-looking logs in every test run. | Spy/silence `console.error` in the no-key test while asserting the warning path, or avoid logging for the intentionally absent-key branch. |

#### Autonomous tasks

| Task ID | priority | exact test files | RED condition | implementation hint | verification command | safe unattended yes/no |
|---|---|---|---|---|---|---|
| AT-01 | P0 | `vitest.config.ts`; optionally `src/test/vitest-config.test.ts` | Temporarily set `include` to an empty glob or delete/rename tests: `npm test` must fail instead of passing with no tests. | Change `passWithNoTests` to `false`; if adding a config test, parse `vitest.config.ts` and assert false. | `npm test` | yes |
| AT-02 | P0 | `vitest.config.ts` | `npm run test:coverage` must fail if global coverage is below agreed floor; current 32.8/29.6/28.6 should fail if thresholds are set above it. | Add `coverage.thresholds` with an initial floor agreed by owner (or start at current minus small margin and ratchet); add `text`/`text-summary` reporter for visible output. | `npm run test:coverage` | no - threshold values require product/owner acceptance |
| AT-03 | P0 | `vitest.config.ts`, `src/setupTests.ts`, `src/test/setup.ts`, `src/test/msw/server.ts` | A hook/API test with an unhandled `fetch('/api/unknown')` should fail under Vitest. | Import shared setup from `src/setupTests.ts`; create `setupServer(...handlers)`; add `beforeAll(() => server.listen({ onUnhandledRequest: 'error' }))`, `afterEach(() => server.resetHandlers())`, `afterAll(() => server.close())`. | `npm test` | yes |
| AT-04 | P1 | `package.json` plus optionally `.github/workflows/ci.yml` | Running the new local quality command should execute audit, strict lint, unit, coverage, build, and e2e in the same order as CI. | Add `quality:ci`; optionally change CI to call it for test/build/e2e parity where artifact boundaries permit. | `npm run quality:ci` | no - long-running and may need CI workflow design approval |
| AT-05 | P1 | `playwright.config.ts` | A spec containing `test.only` should be rejected locally by the normal E2E/quality gate. | Set `forbidOnly: true` or add `e2e:ci` with `playwright test --forbid-only`; add list/html/junit reporters. | `npm run e2e` and `CI=true npm run e2e` | yes |
| AT-06 | P1 | `e2e/wallet.spec.ts` | Remove the sleeps/evaluate helpers; tests still pass and fail if Connect Wallet/options are not accessible by role/name. | Replace `clickWalletButton`/`clickWalletOption` with `await expect(page.getByRole(...)).toBeVisible()` then `.click()`; replace body text scans with role/text expectations. | `npx playwright test e2e/wallet.spec.ts` | yes |
| AT-07 | P1 | `e2e/comprehensive.spec.ts` | If app makes only one request in `retries failed requests`, the test must fail. | Use `expect.poll(() => requestCount).toBe(3)` or wait for the 200 response and assert `requestCount === 3`; remove `waitForTimeout(5000)`. | `npx playwright test e2e/comprehensive.spec.ts -g "retries failed requests"` | yes |
| AT-08 | P2 | `e2e/dashboard-navigation.spec.ts` | If active Risk link has no active class/`aria-current`, the active-page test must fail. | Add an accessible current marker in component if needed; assert `toHaveAttribute('aria-current', 'page')` or a stable active class. | `npx playwright test e2e/dashboard-navigation.spec.ts -g "highlights active"` | yes |
| AT-09 | P2 | `e2e/homepage.spec.ts` | Replacing/removing the Shield feature icon while another SVG remains should fail. | Add `data-testid`/aria-label to the intended icon or assert the full feature card by role/name; avoid `locator('svg').first()`. | `npx playwright test e2e/homepage.spec.ts -g "Shield"` | yes |
| AT-10 | P0 | `src/components/dashboard/transaction-composer.test.tsx` | Mutating memo generation, disabling validation, copy failure, or sign/broadcast error handling should fail. | Mock `next/navigation`, `useWallet`, clipboard, and `executeBondTransaction`/`executeUnbondTransaction`; render positions and cover BOND/UNBOND, query params, disabled submit, preview, copy success/failure, and network mismatch. | `npx vitest run src/components/dashboard/transaction-composer.test.tsx --coverage` | yes |
| AT-11 | P1 | `src/components/dashboard/bond-simulator.test.tsx` | Changing APY math, preset values, min-bond warning, or invalid input handling should fail. | Render simulator with current positions; test default result, preset click, invalid/zero values, `NETWORK.MINIMUM_BOND_RUNE` warning, and typo-safe aggressive preset behavior. | `npx vitest run src/components/dashboard/bond-simulator.test.tsx --coverage` | yes |
| AT-12 | P1 | `src/lib/hooks/__tests__/use-wallet.test.ts` | Removing Vultisig support or leaking `window.vultisig/window.thorchain` between tests should fail. | Add cleanup for `vultisig` and `thorchain`; add detection/connect tests for `window.vultisig.thorchain` and legacy `window.thorchain`. | `npx vitest run src/lib/hooks/__tests__/use-wallet.test.ts` | yes |
| AT-13 | P0 | `src/app/api/address/[address]/route.test.ts` | Invalid addresses, rate-limited requests, limit >1000, non-bond actions, unsorted dates, malformed amounts, or upstream exceptions should fail their expected contract. | Mock rate-limit module and Midgard; add table-driven tests around `route.ts:23-86` behavior and assert headers/status/body. | `npx vitest run 'src/app/api/address/[address]/route.test.ts'` | yes |
| AT-14 | P1 | `src/lib/hooks/__tests__/use-watchlist.test.ts`, `src/lib/api/__tests__/coinapi.test.ts` | `npm test` should not print stderr for expected invalid localStorage JSON or missing CoinAPI key. | Use `vi.spyOn(console, 'error').mockImplementation(() => {})` in the specific negative-path tests and assert call count/message. | `npm test 2> /tmp/heimdall-vitest-stderr.log; test ! -s /tmp/heimdall-vitest-stderr.log` | yes |
| AT-15 | P2 | `src/app/api/thorchain/__tests__/route.test.ts`, `src/app/api/midgard/__tests__/route.test.ts` | A successful proxy response missing `Cache-Control` or security headers should fail. | Add assertions for `Cache-Control` max-age on 200s and no cache header on 4xx/5xx; assert shared security headers for success/error/OPTIONS. | `npx vitest run src/app/api/thorchain/__tests__/route.test.ts src/app/api/midgard/__tests__/route.test.ts` | yes |
| AT-16 | P2 | `src/components/shared/api-health-banner.test.tsx` or extend existing API-health component tests | Removing the dismiss button, status messages, or a11y role/test ids should fail without running full E2E. | Render `ApiHealthBanner` directly with degraded/healthy states; assert role/status messaging and dismiss callback. | `npx vitest run src/components/shared/api-health-banner.test.tsx` | yes |
| AT-17 | P2 | `src/lib/hooks/__tests__/use-api-health.test.ts` | Three consecutive failures must mark an API down; fewer failures must not; recovery should mark healthy. | Use MSW or mocked `getHealth`/`getAllNodes` with fake timers/SWR config to test failure counter and recovery semantics. | `npx vitest run src/lib/hooks/__tests__/use-api-health.test.ts` | yes |
| AT-18 | P2 | `src/lib/utils/health-score.test.ts`, `src/lib/utils/portfolio-alerts.test.ts`, `src/lib/utils/bond-optimizer.test.ts` | Mutating core risk/alert/optimizer thresholds should fail. | Add pure-function boundary tests for grade thresholds, generated actionable alerts, and optimization suggestion ranking. | `npx vitest run src/lib/utils/health-score.test.ts src/lib/utils/portfolio-alerts.test.ts src/lib/utils/bond-optimizer.test.ts` | yes |


### A.5 CI/CD deployment/docs

Source temp report: `/tmp/heimdall-audit-infra-docs.md`

### CI/CD deployment docs audit

Audited `/Users/reidar/Projectos/Heimdall` on branch `staging` read-only. YAML syntax checks passed for the scoped workflow, action, compose, Ansible, and inventory files; production `npm audit --omit=dev --audit-level=moderate` returned 0 vulnerabilities; test inventory matches README's `190` Vitest tests and `62` Playwright tests.

#### Findings

| ID | Severity | file:line | evidence | operational impact | autonomous fix |
|---|---|---|---|---|---|
| INFRA-01 | High | `.github/workflows/opencode.yml:3` | Workflow runs on public comment events, gates only on comment body at lines 11-15, grants `id-token: write` at lines 17-21, and executes `anomalyco/opencode/github@latest` with `OLLAMA_API_KEY` at lines 28-33. | Any eligible comment can invoke a mutable third-party action in the base repo context. A compromised or changed `@latest` action could exfiltrate the OLLAMA secret or mint OIDC tokens. | Pin `anomalyco/opencode/github` to a reviewed commit SHA, remove `id-token: write` unless explicitly required, add actor/association allowlisting, and set a short job timeout. |
| INFRA-02 | High | `.github/workflows/ci.yml:91` | Staging and PR refs only run non-pushing `docker-build` at lines 91-121. The only publishing job runs on `push` to `master` at lines 123-127. Ansible defaults to `sha-<local HEAD>` at `ansible-playbook.yml:7-8` and pulls that tag at lines 29-33. | A `staging` candidate can pass CI but have no corresponding GHCR image for the default Ansible deploy. Operators must either push `master`, override `IMAGE_TAG`, or hit a safe-but-blocking missing-image deploy. | Add an immutable staging publish path or add a deploy preflight that refuses to deploy a SHA without a successful CI run and GHCR tag. Keep production deployment gated separately. |
| INFRA-03 | Medium | `.github/workflows/ci.yml:111` | Docker verification tags `ghcr.io/reedtrullz/heimdall`, while publish metadata uses `ghcr.io/${{ github.repository }}` at line 152 and the summary echoes the same at line 184. The canonical repo is documented as `Reedtrullz/Heimdall` in `README.md:5`. | If `${{ github.repository }}` preserves mixed case, the master publish path may emit an invalid or inconsistent image reference while the non-push build tests a lower-case reference. | Define one lower-case `IMAGE_REPOSITORY=ghcr.io/reedtrullz/heimdall` env value and use it in docker-build, metadata, summary, Ansible docs, and compose docs. |
| INFRA-04 | Medium | `.github/workflows/ci.yml:103` | CI truncates deploy identity to `sha-${GITHUB_SHA::7}` at lines 103 and 146. Ansible accepts 7-40 lowercase hex at `ansible-playbook.yml:14`; compose also documents 7-40 at `compose.production.yml:5`. | Seven-character tags are convenient but collision-prone over time and remain mutable by registry convention unless digest verification is added. Rollback and audit trails are weaker than a full SHA or digest. | Switch CI to `sha-${GITHUB_SHA}` or at least 12+ chars, publish/report the digest, and let Ansible accept either a full SHA tag or an explicit digest. |
| INFRA-05 | High | `.github/actions/install-deps/action.yml:24` | The composite action and Dockerfile install Linux native packages with unversioned `npm install --no-save --no-package-lock` at action lines 24-30 and Dockerfile lines 31-37. Runtime Docker install also redirects errors and continues with `|| true` at `Dockerfile:58-62`. | Builds are not fully reproducible; native packages can drift from their parent package versions, and runtime image native dependency failures can be silently hidden. | Lock platform native optional dependencies deterministically, or use npm platform flags with a lockfile strategy. Remove the runtime `|| true` and fail the image build when required native binaries cannot be installed. |
| INFRA-06 | Medium | `Dockerfile:4` | Dockerfile declares public build args at lines 4-12 and bakes them before `npm run build` at lines 14-22 and 39-40. CI passes only a subset at `.github/workflows/ci.yml:114-121` and 166-173. Ansible sets `NEXT_PUBLIC_*` at runtime at `ansible-playbook.yml:63-67`, and `DEPLOYMENT.md:47-51` advertises runtime overrides. | Operators may believe Ansible overrides change the browser bundle, but `NEXT_PUBLIC_*` values are mostly build-time for the client. The shipped image can differ from runtime documentation, especially for `NEXT_PUBLIC_TRACK_API` and `NEXT_PUBLIC_APP_URL`. | Separate build-time public config from runtime server config in docs and playbook. Pass all declared build args in CI or remove unused args, and avoid runtime `NEXT_PUBLIC_*` overrides unless server code truly reads them. |
| INFRA-07 | Low | `compose.production.yml:17` | Compose uses `NEXT_PUBLIC_THORCHAIN_RPC=https://rpc.thorchain.network` at line 17 and `NEXT_PUBLIC_COINGECKO_API=https://api.coingecko.com` at line 20. CI and `.env.example` use `https://rpc.thorchain.info` and `https://api.coingecko.com/api/v3` at `.github/workflows/ci.yml:118,121` and `.env.example:7,10`. | The manual diagnostic path is not representative of the CI-built image and can mislead troubleshooting or produce different server-side defaults if code reads runtime env. | Normalize endpoint defaults across CI, compose, `.env.example`, README, and DEPLOYMENT, preferably from one documented source of truth. |
| INFRA-08 | Medium | `Dockerfile:2` | Runtime base images use mutable `node:22-slim` at lines 2 and 43. CI and opencode use mutable `ubuntu-latest` at `.github/workflows/ci.yml:25,52,72,95,127` and `.github/workflows/opencode.yml:16`. | Rebuilding the same commit can silently change OS, Node patch, native libraries, and cache behavior. This weakens reproducibility and incident rollback. | Pin base images to digests and use a fixed runner label such as `ubuntu-24.04`; add Dependabot or Renovate updates for Docker and Actions. |
| INFRA-09 | Medium | `ansible-playbook.yml:91` | Rollback starts `previous_image` at lines 91-125, then immediately fails with a rollback message at lines 127-130. There is no post-rollback health poll or status capture. | A failed deployment can be reported as rolled back even if the rollback container is unhealthy, cannot start, or serves the wrong version. | Add a rollback health-check block that polls `/api/health`, verifies `VERSION`, and fails with logs if rollback did not restore service. |
| INFRA-10 | Low | `Dockerfile:66` | Dockerfile and Ansible healthchecks probe `http://localhost:3000/api/health` at `Dockerfile:66-67` and `ansible-playbook.yml:52-53,103-104`, while compose uses `127.0.0.1` at `compose.production.yml:25`. | `localhost` can resolve differently across images and hosts. A false healthcheck failure could trigger rollback even when the app is listening on IPv4. | Use `127.0.0.1` consistently and attach an explicit request `.on('error', ...)` handler in every Node healthcheck command. |
| INFRA-11 | Low | `.github/workflows/ci.yml:14` | Top-level workflow permissions include `checks: write` at lines 14-17, but the scoped jobs only need checkout, audit, tests, artifacts, and package publish. Most jobs also lack `timeout-minutes`; only `npm test` has one at line 39. | Least-privilege is not tight, and hung Playwright, build, Docker, or publish steps can consume runner time until GitHub's default limit. | Remove unused `checks: write` unless a step proves it is needed, and add job-level timeouts for test, build, e2e, docker-build, publish, and opencode. |
| INFRA-12 | Medium | `CLAUDE.md:73` | CLAUDE documents a three-stage Dockerfile with `deps`, `builder`, and `runner` at lines 73-81, but the actual Dockerfile has only `builder` and `runner` at `Dockerfile:1-43`. Older plans also contain obsolete instructions: `docs/plans/2026-05-05-heimdall-next-steps.md:119-131` expects `version: latest`, line 171 says `CI/CD Pipeline`, and `docs/plans/2026-05-24-audit-critical-fixes.md:517-568` references `.github/workflows/ci-cd.yml` and old Alpine examples. | Autonomous agents following stale docs/plans can reintroduce old workflow names, mutable tags, Alpine Dockerfiles, or `latest` deployment expectations. | Add superseded banners to old plans or move them to archive, and align CLAUDE/AGENTS/README/DEPLOYMENT with the actual two-stage Dockerfile and current `ci.yml`. |

#### Autonomous tasks

| Task ID | priority | exact files | steps | validation commands | docs-only? | safe unattended yes/no |
|---|---|---|---|---|---|---|
| TASK-01 | P0 | `.github/workflows/opencode.yml` | Pin `anomalyco/opencode/github` to a reviewed commit SHA; remove `id-token: write` unless the action documentation proves it is needed; add `github.event.comment.author_association` allowlist for OWNER, MEMBER, COLLABORATOR; add `timeout-minutes: 10`; document the policy in a workflow comment. | `python3 - <<'PY'` load workflow YAML with PyYAML; `git diff -- .github/workflows/opencode.yml`; if possible run `gh workflow view opencode --yaml` after push. | no | no |
| TASK-02 | P0 | `.github/workflows/ci.yml`, `ansible-playbook.yml`, `README.md`, `DEPLOYMENT.md`, `CLAUDE.md`, `AGENTS.md` | Introduce a single lower-case `IMAGE_REPOSITORY`; publish immutable images for `staging` as well as `master` or add an Ansible preflight that checks the selected tag exists and has a successful CI run; keep production deployment gated and do not add `latest`. | `python3 - <<'PY'` YAML-load `.github/workflows/ci.yml`; `gh workflow run CI --ref staging` in a test branch or inspect generated workflow; `IMAGE_TAG=sha-<known> ansible-playbook -i inventory/hosts.yml ansible-playbook.yml --syntax-check`. | no | no |
| TASK-03 | P1 | `.github/workflows/ci.yml`, `ansible-playbook.yml`, `compose.production.yml`, `DEPLOYMENT.md`, `README.md` | Replace 7-char deploy tags with full `sha-${GITHUB_SHA}` or a documented 12+ char minimum; include `${{ steps.build.outputs.digest }}` in the publish summary; optionally allow `IMAGE_DIGEST` deploys; update regexes and docs to match. | `python3 - <<'PY'` YAML-load CI, compose, and Ansible; `grep -R "GITHUB_SHA::7\|{7,40}\|short SHA" .github ansible-playbook.yml compose.production.yml DEPLOYMENT.md README.md`. | no | yes |
| TASK-04 | P1 | `.github/actions/install-deps/action.yml`, `Dockerfile`, `package.json`, `package-lock.json`, `CLAUDE.md`, `AGENTS.md` | Make Linux native optional dependencies deterministic: pin exact platform package versions matching parent packages or generate a lockfile that includes linux-x64 optional packages; remove unversioned `npm install --no-save --no-package-lock`; remove Dockerfile runtime `2>/dev/null || true`; document the update procedure. | `npm ci --include=optional`; `npm run lint -- --max-warnings=0`; `npm test`; `npm run build`; `docker build --platform linux/amd64 --build-arg VERSION=ci-test -t heimdall:native-deps .`. | no | yes |
| TASK-05 | P1 | `Dockerfile`, `.github/workflows/ci.yml`, `.github/workflows/opencode.yml`, optional `.github/dependabot.yml` | Pin `node:22-slim` by digest; switch workflow runners from `ubuntu-latest` to a fixed supported runner; add Docker/GitHub Actions update automation; ensure docs mention why Alpine remains forbidden. | `docker build --pull --platform linux/amd64 -t heimdall:pinned .`; `python3 - <<'PY'` YAML-load workflows; `grep -R "ubuntu-latest\|node:22-slim" Dockerfile .github/workflows`. | no | yes |
| TASK-06 | P1 | `.github/workflows/ci.yml`, `Dockerfile`, `ansible-playbook.yml`, `compose.production.yml`, `.env.example`, `README.md`, `DEPLOYMENT.md`, `CLAUDE.md` | Define the build-time/runtime env contract: pass every intended public build arg in CI, remove runtime-only `NEXT_PUBLIC_*` entries that cannot affect the client, keep server-only proxy vars in Ansible/compose, and update override docs to distinguish build-time from runtime. | `npm run build`; `docker build --build-arg NEXT_PUBLIC_TRACK_API=https://example.invalid --build-arg VERSION=env-contract -t heimdall:env-contract .`; `grep -R "NEXT_PUBLIC_TRACK_API\|NEXT_PUBLIC_APP_URL\|Override variables" .github Dockerfile ansible-playbook.yml compose.production.yml .env.example README.md DEPLOYMENT.md CLAUDE.md`. | no | yes |
| TASK-07 | P2 | `compose.production.yml`, `.env.example`, `README.md`, `DEPLOYMENT.md`, optional `scripts/validate-compose-env.sh` | Align compose endpoints with CI and `.env.example`; add a tiny validation script or documented command that rejects non-hex `IMAGE_SHA` before `docker compose`; clarify compose is diagnostic only. | `docker compose -f compose.production.yml config` with `IMAGE_SHA=deadbeef`; `grep -R "rpc.thorchain.network\|api.coingecko.com$" compose.production.yml .env.example README.md DEPLOYMENT.md`. | mostly | yes |
| TASK-08 | P2 | `ansible-playbook.yml`, `DEPLOYMENT.md` | Convert deploy/rollback to an Ansible `block` plus `rescue` or add explicit post-rollback health polling; verify rollback `VERSION` matches `previous_version`; use `127.0.0.1` in container healthchecks; include recent container logs in failure output. | `ansible-playbook -i inventory/hosts.yml ansible-playbook.yml --syntax-check`; if a non-prod target exists, run with a deliberately bad `IMAGE_TAG` and confirm service rolls back healthy. | no | no |
| TASK-09 | P2 | `Dockerfile`, `ansible-playbook.yml`, `compose.production.yml` | Standardize all container healthchecks on `http://127.0.0.1:3000/api/health` and add Node request error handlers in Dockerfile and Ansible healthcheck commands. | `docker build -t heimdall:healthcheck .`; `docker inspect heimdall:healthcheck --format '{{json .Config.Healthcheck}}'`; `ansible-playbook -i inventory/hosts.yml ansible-playbook.yml --syntax-check`. | no | yes |
| TASK-10 | P2 | `.github/workflows/ci.yml`, `.github/workflows/opencode.yml` | Remove unused top-level `checks: write` or scope it to a job that proves need; add job-level `timeout-minutes` for test, build, e2e, docker-build, publish, and opencode. | `python3 - <<'PY'` YAML-load both workflows; `grep -R "checks: write\|timeout-minutes" .github/workflows`. | no | yes |
| TASK-11 | P2 | `CLAUDE.md`, `AGENTS.md`, `README.md`, `DEPLOYMENT.md`, `docs/plans/2026-05-05-heimdall-next-steps.md`, `docs/plans/2026-05-24-audit-critical-fixes.md`, `docs/plans/2026-06-06-heimdall-release-trust-sprint.md` | Add `SUPERSEDED/HISTORICAL PLAN - DO NOT EXECUTE VERBATIM` banners to obsolete plans; fix CLAUDE's Docker stage description; remove `latest`, `ci-cd.yml`, `CI/CD Pipeline`, and Alpine instructions from active docs or mark them as historical anti-patterns. | `grep -R "latest\|ci-cd.yml\|CI/CD Pipeline\|node:22-alpine\|three stages\|deps" CLAUDE.md AGENTS.md README.md DEPLOYMENT.md docs/plans`; `npm run lint -- --max-warnings=0` only if docs lint hooks exist. | yes | yes |
| TASK-12 | P3 | `inventory/hosts.yml`, `DEPLOYMENT.md`, optional `inventory/staging.yml`, optional `inventory/production.yml` | Split staging and production targets if both exist, or add explicit `--limit vps` and confirmation guidance so the default local command cannot be mistaken for a harmless dry run; keep real secrets in vault only. | `ansible-inventory -i inventory/hosts.yml --list`; `ansible-playbook -i inventory/hosts.yml ansible-playbook.yml --syntax-check`; docs grep for the exact deploy command. | mostly | no |


### A.6 Maintainability/code quality

Source temp report: `/tmp/heimdall-audit-maintainability.md`

### Maintainability/code-quality audit

Scope evidence gathered from branch `staging` under `/Users/reidar/Projectos/Heimdall` with read-only source inspection, import graph scripts, `npm run lint`, `npx tsc --noEmit --pretty false`, and `npx --yes ts-prune --project tsconfig.json`. Lint and TypeScript currently pass; this report focuses on maintainability risks that can be fixed autonomously.

#### Findings

| ID | Severity | file:line | evidence | maintainability impact | autonomous fix |
|---|---|---|---|---|---|
| MNT-001 | High | `src/app/dashboard/changelogs/page.tsx:104` | Changelogs page is 666 lines with 181 JSX tag starts, 6 effects, 5 memos, localStorage parsing/persistence, URL syncing, filtering, highlighting, year nav, and entry rendering in one client component. | High-change page is hard to reason about; every UI tweak risks storage or URL sync regressions. | Extract pure helpers to `src/lib/utils/changelogs.ts`; extract `ChangelogToolbar`, `ChangelogYearNav`, `ChangelogMonth`, `ChangelogEntryCard`, and `useChangelogExpandedState`. |
| MNT-002 | High | `src/app/dashboard/risk/page.tsx:54`, `src/app/dashboard/risk/page.tsx:191`, `src/app/dashboard/risk/page.tsx:455` | Risk page is 530 lines; it declares `RiskSummaryBanner`, `NodesList`, severity scoring, and page rendering in the route file. | Route files become component libraries, increasing re-render/debug scope and discouraging focused tests. | Move `RiskSummaryBanner` and `NodesList` into `src/components/dashboard/risk/`; move scoring/formatting to `src/lib/utils/risk-display.ts`. |
| MNT-003 | High | `src/lib/api/midgard.ts:453`, `src/lib/api/midgard.ts:515`, `src/lib/api/midgard.ts:598` | `midgard.ts` is 604 lines / 22 functions and mixes earnings, network, THORName, LP member, rune price, and pool-history domains. It is also the #3 most-imported module (32 imports). | High-centrality monolith makes endpoint changes risky and increases accidental coupling. | Split into domain files (`midgard/network.ts`, `midgard/pricing.ts`, `midgard/lp.ts`, `midgard/thorname.ts`) with a compatibility barrel. |
| MNT-004 | High | `src/lib/hooks/use-lp-positions.ts:129`, `src/lib/hooks/use-lp-positions.ts:151`, `src/lib/hooks/use-lp-positions.ts:166` | LP hook is 405 lines and does member, pool, rune price, per-pool THORNode LP, and per-pool historical pricing fetches in one SWR fetcher. | Data fetching, progress UI, and valuation are entangled; large addresses can fan out many upstream calls and make failures hard to localize. | Split into `useLpRawData`, `useLpHistoricalPricing`, and pure `buildLpPositions`; add bounded concurrency for per-pool historical calls. |
| MNT-005 | High | `src/lib/hooks/use-lp-positions.ts:131`, `src/lib/hooks/use-lp-positions.ts:157` | SWR fetcher calls `setLoadingProgress` while resolving pool promises. | React state side effects inside fetchers are brittle under dedupe, aborts, unmounts, and React 19 concurrent rendering. | Remove progress state from the fetcher or move progress to a separate imperative loader with cancellation; otherwise return progress metadata from an isolated async helper. |
| MNT-006 | Medium | `src/lib/hooks/use-lp-positions.ts:132` | LP SWR key is the bare address string (`address ? address : null`) instead of a namespaced tuple. | Bare keys can collide with unrelated cache entries and make global mutate/invalidation unpredictable. | Change to `['lp-positions', address]` and update tests/global refresh. |
| MNT-007 | Medium | `src/components/layout/dashboard-shell.tsx:20`, `src/components/layout/dashboard-shell.tsx:71`, `src/lib/hooks/use-bond-history.ts:22`, `src/lib/hooks/use-fee-revenue.ts:6` | Manual refresh mutates only a short string list (`nodes`, `earnings-history`, etc.), while many hooks use tuple keys (`['bond-details', address]`, `['earnings-history', interval, count]`) or omitted keys (`fee-revenue`, `pools`, `yield-benchmarks`, `protocol-version`). | Refresh button gives partial/stale dashboard state and future hooks are easy to forget. | Introduce typed SWR key factories and use predicate `mutate((key) => isDashboardKey(key))` or exact key factories from hooks. |
| MNT-008 | Medium | `eslint.config.mjs:10`, `eslint.config.mjs:11` | React hooks rules `react-hooks/set-state-in-effect` and `react-hooks/preserve-manual-memoization` are globally disabled. | React 19 lint coverage is intentionally reduced; future code can accumulate avoidable effect-derived state and broken memo dependencies. | Re-enable as warnings first, fix surfaced cases in batches, then promote to errors. |
| MNT-009 | Medium | `tsconfig.json:5`, `tsconfig.json:6`, `tsconfig.json:7` | `strict` is true, but `allowJs` and `skipLibCheck` are true; stricter safety flags such as `noUncheckedIndexedAccess` and `exactOptionalPropertyTypes` are not enabled. Exact explicit-`any` scan found 0 app/e2e matches, but test override disables future warnings. | Strictness is good but leaves array/index/optional-property mistakes unguarded and can hide dependency type drift. | Add a stricter secondary config (`tsconfig.strictest.json`) first; then incrementally enable `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, and remove unused `allowJs` if no JS inputs are needed. |
| MNT-010 | Medium | `src/components/dashboard/actionable-alerts.tsx:14`, `src/components/dashboard/apy-chart.tsx:39`, `src/components/dashboard/bond-optimizer.tsx:18`, `src/lib/hooks/use-coinapi-price.ts:12`, `src/lib/utils/export.ts:4` | Import graph and `ts-prune` identify unimported non-entry files/exports including 11 dashboard components plus `use-coinapi-price`, `use-pending-transactions`, `src/lib/utils/export.ts`, `src/lib/utils/pool.ts`, `src/components/ui/index.ts`. | Dead files inflate search surface and invite fixes to code not shipped. | For each candidate, either wire it into an intended page/story or delete with tests; ignore Next route/page false positives from `ts-prune`. |
| MNT-011 | Medium | `src/lib/hooks/use-watchlist.ts:9`, `src/lib/utils/address-validation.ts:3` | THORChain address validation is duplicated and inconsistent: watchlist requires 42-62 chars/Base58-like, utility regex allows `tthor` and different character set/length. | Same address can be accepted in one flow and rejected in another. | Import `isValidTHORChainAddress` from `src/lib/utils/address-validation.ts` everywhere and align tests around one canonical rule. |
| MNT-012 | Low | `src/components/layout/dashboard-shell.tsx:40`, `src/components/shared/recent-addresses.tsx:9`, `src/components/wallet/wallet-connect.tsx:8` | `truncateAddress` is duplicated in 3 components with different thresholds/slice lengths. | Minor display inconsistencies and duplicated future changes. | Add `truncateAddress(address, opts?)` to `src/lib/utils/formatters.ts` and replace local helpers. |
| MNT-013 | Medium | `src/app/api/midgard/[...path]/route.ts:29`, `src/app/api/thorchain/[...path]/route.ts:37`, `src/app/api/coingecko/[...path]/route.ts:17`, `src/app/api/midgard/[...path]/route.ts:33`, `src/app/api/thorchain/[...path]/route.ts:52` | Proxy routes duplicate allowlist checks, success headers, rate-limit response shape, and CORS/cache header merging. | Security/cache behavior can diverge across proxies during future endpoint additions. | Extract `isAllowedProxyPath`, `proxySuccessHeaders`, and `rateLimitResponse` helpers in `src/lib/api/proxy.ts`; keep route-specific allowlists local. |
| MNT-014 | Medium | `src/lib/api/coingecko.ts:10`, `src/lib/api/coinapi.ts:25`, `src/lib/api/midgard.ts:477`, `src/app/dashboard/rewards/page.tsx:125` | There are 9 raw `fetch(` call sites outside the shared `fetchApi` path, with separate retry/error behavior. | Error handling, cache headers, timeout, and retry semantics are inconsistent. | Add a generic `fetchJson`/`fetchText` helper with timeout, typed response, and log policy; route raw calls through it where safe. |
| MNT-015 | Medium | `src/lib/hooks/use-rune-price.ts:14`, `src/lib/hooks/use-network-metrics.ts:5`, `src/lib/hooks/use-fee-revenue.ts:5`, `src/lib/hooks/use-bond-history.ts:21` | 20 hook files import/use SWR; most return hand-built `{ data, error, isLoading, mutate }` shapes and repeat refresh/retry options. | Repeated fetch/error/loading patterns make cache policy drift likely and increase boilerplate for each new endpoint. | Add shared SWR option constants and key factories; optionally add small domain wrappers (`useMidgardSWR`, `useThornodeSWR`) rather than a large abstraction. |
| MNT-016 | Medium | `src/app/dashboard/portfolio/page.tsx:74`, `src/app/dashboard/portfolio/page.tsx:87`, `src/app/dashboard/portfolio/page.tsx:93` | Portfolio page eagerly starts 7 data hooks, including data for collapsed sections (`feeRevenue`, market pools, all nodes, benchmarks), then blocks initial render on most loading states. | Hidden panels can slow first meaningful render and increase API load. | Gate optional hooks on `showFeeRevenue`, `showMarket`, and `showIntelligence`; render skeletons per section instead of one global gate. |
| MNT-017 | Medium | `src/components/dashboard/transaction-composer.tsx:61`, `src/components/dashboard/transaction-composer.tsx:80`, `src/components/dashboard/transaction-composer.tsx:103` | Transaction composer has 8 state atoms, 3 effects, and 10 memos in one 407-line component. | Form behavior is difficult to modify safely; URL-param sync, validation, copy feedback, preview, and signing are coupled. | Extract `useTransactionComposerState`, `useCopyFeedback`, and `TransactionFormFields` / `TransactionResultPanel` components. |
| MNT-018 | Medium | `src/app/dashboard/changelogs/page.tsx:116`, `src/app/dashboard/changelogs/page.tsx:145`, `src/app/dashboard/layout.tsx:42`, `src/app/dashboard/rewards/page.tsx:87` | Direct localStorage/sessionStorage access appears 65/6 times across 17/2 files; storage parsing and migration are handled ad hoc. | SSR guards, parse errors, migrations, and key naming are repeated and easy to get wrong. | Create typed storage utilities (`getJsonStorage`, `setJsonStorage`, `migrateStorageKey`) and key constants, then migrate local call sites. |
| MNT-019 | Low | `src/app/dashboard/changelogs/page.tsx:161`, `src/lib/hooks/use-watchlist.ts:35`, `src/lib/hooks/use-transaction-history.ts:104`, `src/lib/api/coinapi.ts:44` | 29 `console.*` call sites across 18 files; some expected-degraded paths log full errors/warnings. | Console noise makes real production failures harder to spot and encourages inconsistent error handling. | Add a tiny `logger` utility with levels/environment gating and replace degraded-path logs with silent telemetry-friendly handling. |
| MNT-020 | Low | `src/app/learn/layout.tsx:4`, `src/app/learn/page.tsx:33`, `src/app/learn/[slug]/page.tsx:153` | Stale `BondTrack` naming appears in user-facing learn metadata/content. The `BONDTRACK_ADDRESS` storage key is intentionally excluded as the allowed legacy key. | Brand drift makes product copy inconsistent and can confuse support/docs. | Rename visible copy to Heimdall/THORChain Bond Dashboard while preserving `BONDTRACK_ADDRESS`. |
| MNT-021 | Low | `src/data/changelogs.ts:1` | Embedded changelog data is 1,385 lines in a TypeScript module. | Static content dominates code search and TypeScript processing; content edits require code-review semantics. | Move content to JSON/MDX or split by year/month and import via a typed adapter. |
| MNT-022 | Medium | `src/lib/utils/tax-export.ts:57`, `src/lib/api/midgard.ts:244`, `src/lib/utils/calculations.ts:167`, `src/lib/utils/lp-analytics.ts:211` | Amount/parsing/valuation utilities are spread across tax, Midgard, calculations, and LP analytics. Similar helpers use different return types (`bigint` vs `number`). | Financial calculations are harder to audit; duplicated amount conversions risk precision drift. | Create a `src/lib/utils/amounts.ts` module for base-unit parsing/formatting/conversion and migrate domain code gradually. |

#### Autonomous tasks

| Task ID | priority | exact files | steps | verification commands | risk/size | safe unattended yes/no |
|---|---|---|---|---|---|---|
| AT-001 | P0 | `eslint.config.mjs`; files surfaced by lint after change | Change disabled React hooks rules to `warn`; run lint; fix straightforward dependency/set-state cases; leave documented TODO only if rule surfaces ambiguous behavior. | `npm run lint`; `npx tsc --noEmit --pretty false` | Medium / small-to-medium | yes |
| AT-002 | P0 | `src/lib/hooks/use-lp-positions.ts`; `src/lib/hooks/__tests__/use-lp-positions.test.ts`; `src/components/dashboard/lp-*` if progress prop changes | Rename SWR key to `['lp-positions', address]`; remove `setLoadingProgress` from fetcher or isolate with cancellation; keep returned shape compatible. | `npm run lint`; `npx tsc --noEmit --pretty false`; `npm run test -- src/lib/hooks/__tests__/use-lp-positions.test.ts src/app/dashboard/lp/page.test.tsx` | Medium / medium | yes |
| AT-003 | P0 | `src/lib/swr-keys.ts` (new); all files in `src/lib/hooks/*.ts`; `src/components/layout/dashboard-shell.tsx` | Add typed SWR key factory constants/predicates; update hooks to use them; update refresh to mutate all dashboard keys including tuple keys and optional-section keys. | `npm run lint`; `npx tsc --noEmit --pretty false`; `npm run test -- src/lib/hooks/__tests__/*.test.ts src/components/layout/sidebar.test.tsx` | Medium / medium | yes |
| AT-004 | P1 | `src/app/dashboard/portfolio/page.tsx` | Gate optional data hooks behind `showFeeRevenue`, `showMarket`, `showIntelligence`; avoid blocking initial render on collapsed panels; add section-level loading states. | `npm run lint`; `npx tsc --noEmit --pretty false`; `npm run test -- src/components/dashboard/__tests__/pnl-dashboard.test.tsx`; `npm run e2e -- e2e/portfolio.spec.ts` | Medium / medium | yes |
| AT-005 | P1 | `src/app/dashboard/changelogs/page.tsx`; `src/lib/utils/changelogs.ts` (new); `src/components/dashboard/changelogs/*` (new); `src/app/dashboard/changelogs/page.test.tsx` | Move pure filter/query/highlight helpers; extract toolbar/year-nav/month/entry components; keep route page as orchestration only. | `npm run lint`; `npx tsc --noEmit --pretty false`; `npm run test -- src/app/dashboard/changelogs/page.test.tsx` | Medium / medium-large | yes |
| AT-006 | P1 | `src/app/dashboard/risk/page.tsx`; `src/components/dashboard/risk-summary-banner.tsx` (new); `src/components/dashboard/risk-nodes-list.tsx` (new); `src/lib/utils/risk-display.ts` (new) | Move nested components and severity/format helpers out of page; add focused tests for severity sorting/empty state if absent. | `npm run lint`; `npx tsc --noEmit --pretty false`; `npm run test -- src/components/dashboard/__tests__/churn-out-risk.test.tsx` | Medium / medium | yes |
| AT-007 | P1 | `src/components/dashboard/transaction-composer.tsx`; `src/components/dashboard/transaction-composer/*` (new); `src/lib/hooks/use-transaction-composer-state.ts` (new) | Extract URL-param sync/state reducer, copy feedback hook, form fields, preview/result panel; preserve props and DOM labels. | `npm run lint`; `npx tsc --noEmit --pretty false`; `npm run test -- src/components/dashboard/__tests__/transaction-history.test.tsx`; `npm run e2e -- e2e/transactions.spec.ts` | Medium / medium-large | yes |
| AT-008 | P1 | `src/lib/api/proxy.ts` (new); `src/app/api/midgard/[...path]/route.ts`; `src/app/api/thorchain/[...path]/route.ts`; `src/app/api/coingecko/[...path]/route.ts`; route tests | Extract proxy header/rate-limit/allowlist helpers; keep per-route allowlist and Thornode path normalization intact. | `npm run lint`; `npx tsc --noEmit --pretty false`; `npm run test -- src/app/api/midgard/__tests__/route.test.ts src/app/api/thorchain/__tests__/route.test.ts` | Medium / medium | yes |
| AT-009 | P1 | `src/lib/api/client.ts`; `src/lib/api/coingecko.ts`; `src/lib/api/coinapi.ts`; `src/lib/api/midgard.ts`; `src/app/dashboard/rewards/page.tsx`; `src/components/dashboard/tax-export.tsx`; `src/components/shared/export-button.tsx` | Add shared fetch helpers for JSON/text/blob with timeout and consistent error messages; migrate raw fetch sites that do not require route-specific behavior. | `npm run lint`; `npx tsc --noEmit --pretty false`; `npm run test -- src/lib/api/__tests__/*.test.ts src/lib/utils/__tests__/tax-export.test.ts` | Medium / medium | yes |
| AT-010 | P1 | `src/lib/utils/address-validation.ts`; `src/lib/hooks/use-watchlist.ts`; `src/lib/hooks/__tests__/use-watchlist.test.ts`; `src/components/shared/address-input.tsx` if it has local validation | Replace local watchlist validator with canonical utility; align test cases for mainnet/testnet/invalid addresses. | `npm run lint`; `npx tsc --noEmit --pretty false`; `npm run test -- src/lib/hooks/__tests__/use-watchlist.test.ts` | Low / small | yes |
| AT-011 | P2 | `src/lib/utils/formatters.ts`; `src/components/layout/dashboard-shell.tsx`; `src/components/shared/recent-addresses.tsx`; `src/components/wallet/wallet-connect.tsx`; formatter tests | Add shared `truncateAddress`; replace local helpers; assert default and wallet-short variants. | `npm run lint`; `npx tsc --noEmit --pretty false`; `npm run test -- src/lib/utils/formatters.test.ts` | Low / small | yes |
| AT-012 | P2 | `src/lib/storage.ts` (new); `src/app/dashboard/layout.tsx`; `src/app/page.tsx`; `src/app/dashboard/changelogs/page.tsx`; `src/components/alerts/alert-toast.tsx`; `src/components/dashboard/pnl-dashboard.tsx`; `src/lib/hooks/use-alerts.ts`; `src/lib/hooks/use-pending-transactions.ts`; `src/lib/hooks/use-wallet.ts`; `src/lib/hooks/use-watchlist.ts` | Add typed local/session storage helpers and storage key constants; migrate direct JSON parsing and key migration logic; keep `BONDTRACK_ADDRESS` unchanged. | `npm run lint`; `npx tsc --noEmit --pretty false`; `npm run test -- src/lib/hooks/__tests__/use-wallet.test.ts src/lib/hooks/__tests__/use-watchlist.test.ts src/components/alerts/__tests__/alert-toast.test.tsx src/components/dashboard/__tests__/pnl-dashboard.test.tsx` | Medium / medium-large | yes |
| AT-013 | P2 | `src/lib/logger.ts` (new); all files with `console.*` found by scan | Add environment-gated logger; replace console noise; keep error boundary logging if desired but route through logger. | `python3 - <<'PY'\nimport pathlib,re\nprint(sum(len(re.findall(r"console\\.", p.read_text())) for p in pathlib.Path("src").rglob("*.ts*") if p.is_file()))\nPY`; `npm run lint`; `npx tsc --noEmit --pretty false` | Low / small-medium | yes |
| AT-014 | P2 | `src/app/learn/layout.tsx`; `src/app/learn/page.tsx`; `src/app/learn/[slug]/page.tsx` | Replace visible `BondTrack` copy with current Heimdall naming; do not rename `BONDTRACK_ADDRESS`. | `python3 - <<'PY'\nimport pathlib,re\nfor p in pathlib.Path("src").rglob("*.ts*"):\n    txt=p.read_text()\n    if "BondTrack" in txt and "BONDTRACK_ADDRESS" not in txt: print(p)\nPY`; `npm run lint`; `npx tsc --noEmit --pretty false` | Low / small | yes |
| AT-015 | P2 | Candidate files from MNT-010; dependent tests/importers | For each unimported non-entry file, decide keep/delete by product intent; delete only after import graph confirms no references and tests are updated. | `npx --yes ts-prune --project tsconfig.json`; `npm run lint`; `npx tsc --noEmit --pretty false`; `npm run test` | Medium / medium | no |
| AT-016 | P2 | `tsconfig.strictest.json` (new); `package.json`; files surfaced by strictest compile | Add `typecheck:strictest`; enable `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `noImplicitOverride`; initially non-CI if many findings. | `npm run typecheck:strictest`; `npm run lint`; `npx tsc --noEmit --pretty false` | Medium / medium-large | yes for config/add script, no for broad fixes |
| AT-017 | P3 | `src/data/changelogs.ts`; new content files under `src/data/changelogs/`; `src/lib/hooks/use-changelogs.ts`; changelog tests | Split embedded changelog content by year/month or JSON; add typed loader/adapter preserving `ChangelogItem[]`. | `npm run lint`; `npx tsc --noEmit --pretty false`; `npm run test -- src/app/dashboard/changelogs/page.test.tsx` | Medium / medium | yes |
| AT-018 | P3 | `src/lib/utils/amounts.ts` (new); `src/lib/api/midgard.ts`; `src/lib/utils/tax-export.ts`; `src/lib/utils/calculations.ts`; `src/lib/utils/lp-analytics.ts`; related tests | Centralize base-unit parsing/conversion; migrate one domain at a time; keep `number` display conversions explicit. | `npm run lint`; `npx tsc --noEmit --pretty false`; `npm run test -- src/lib/utils/__tests__/tax-export.test.ts src/lib/utils/__tests__/lp-analytics.test.ts src/lib/utils/__tests__/calculations.test.ts` | Medium / medium-large | yes |
