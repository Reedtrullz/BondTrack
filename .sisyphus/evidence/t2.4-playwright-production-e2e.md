# T2.4 — Update Playwright config for production-build E2E

## Changes Made

### playwright.config.ts
- `webServer.command`: `'npm run dev'` → `'npm run build && npm start'`
- `webServer.timeout`: `120000` → `180000` (3 minutes to allow for build time)
- `reuseExistingServer: !process.env.CI` preserved

### e2e/AGENTS.md
- Updated Base URL section to document production-build E2E setup
- Added note about 180s timeout and CI vs local behavior

## Verification

```
npm run e2e
> playwright test

[WebServer] ⚠ "next start" does not work with "output: standalone" configuration. Use "node .next/standalone/server.js" instead.

Running 61 tests using 5 workers
...
61 passed (38.8s)
```

All 61 tests pass against the production build.

## Notes

- Next.js emits a warning that `next start` is not optimal with `output: 'standalone'`, but the server starts and all tests pass.
- The Dockerfile uses `CMD ["node", "server.js"]` for true production parity; the E2E webServer uses `npm start` for simplicity and test coverage.
- Build time on this machine: ~30-40s (cached). First build may take longer.
