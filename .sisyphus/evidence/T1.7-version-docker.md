# T1.7 Evidence

- Updated `Dockerfile` to pass `VERSION` through builder and runner stages.
- Verified `src/app/api/health/route.ts` already returns `process.env.VERSION || 'unknown'`.
- Attempted local Docker build:

```text
docker build --build-arg VERSION=test-123 -t heimdall-test .
zsh:1: command not found: docker
```

- Source build still passes with the Dockerfile change in place: `npm run build` completed successfully.
