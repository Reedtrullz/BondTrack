# T1.3 Dashboard layout store refactor

- Replaced `useSyncExternalStore` with `useState` + `useEffect` in `src/app/dashboard/layout.tsx`.
- Preserved address restore/migration behavior for `BONDTRACK_ADDRESS`, `dashboard-address`, and `thornode-watcher-last-address`.
- Kept URL `?address=` syncing behavior intact.

## Verification

- `npm test` ✅
- `npm run build` ✅
- `lsp_diagnostics` on `src/app/dashboard/layout.tsx` ✅ clean
