# T1.6 npm audit CI evidence

## Workflow change
- Added `npm audit --omit=dev --audit-level=moderate` to `.github/workflows/ci.yml` in the `test` job.
- Placement: after `npm ci` via `install-deps`, before `npm test`.
- Added a comment explaining the moderate+ gate and devDependency omission.

## Verification
- Local audit: `found 0 vulnerabilities`
- Workflow YAML parse: `YAML OK`
